# 05 传感器方案

## 1. 传感器配置与分工

| 传感器 | 数量 | 输出接口 | 安装位置 | 职责 |
|---|---|---|---|---|
| 超声波测距 | 8 | **UART** | 整车四周（前 4 + 后 4） | 近距离避障（~0.2~4m），贴边/低速场景 |
| 激光测距 | 4 | **UART** | 前 2 + 后 2，**斜向下照射** | 检测前后路面坑洞、台阶落差 |

送餐车特有需求：上人行道、过路沿石 → 激光测距负责提前发现路面落差。

## 2. 接入架构（分布式汇聚到桥端）

传感器跟着物理位置走，分挂前后桥 MCU（S32K344，各 16 路 LPUART 富余）：

```
前桥板 S32K344                      后桥板 S32K344
├─ LPUART0 ─ 超声波1                ├─ LPUART0 ─ 超声波5
├─ LPUART1 ─ 超声波2                ├─ LPUART1 ─ 超声波6
├─ LPUART2 ─ 超声波3                ├─ LPUART2 ─ 超声波7
├─ LPUART3 ─ 超声波4                ├─ LPUART3 ─ 超声波8
├─ LPUART4 ─ 激光测距·前左          ├─ LPUART4 ─ 激光测距·后左
└─ LPUART5 ─ 激光测距·前右          └─ LPUART5 ─ 激光测距·后右
```

**优点**：
- 传感器离采集点最近，UART 走线最短（单端信号怕长距离串扰）
- 负载分摊，VCU 只收打包后的 CAN 报文
- 前桥故障不影响后侧传感器（故障域隔离）

## 3. 软件设计要点（桥端实现）

### 3.1 接收机制

- **DMA + 空闲行中断**接收：不占用 CPU 逐字节中断，DMA 收整帧、空闲中断触发解析
- 每路传感器独立状态机：收帧 → 校验 → 滤波（中值/限幅）→ 更新距离值 + 时间戳

### 3.2 数据有效性规则

1. **超时标无效**：500ms 无新数据 → 状态位置"无效"
2. **无效 ≠ 无障碍**：上报时区分"无效"与"畅通"；避障逻辑对无效值按**降速确认**处理，绝不当作前方畅通
3. **帧校验**：协议有校验位必须验；无校验位做长度 + 范围合理性检查（激光报 0mm 或满量程突变多为乱码，滤掉）
4. **数据冻结检测**：连续多帧完全相同的值（传感器卡死）也要标记异常

### 3.3 上报格式（进 CAN 报文矩阵定稿）

- 每桥打包上报：6 个距离值 + 6 个有效标志位（1~2 条 CAN 报文，20~50Hz）
- 异常事件单独上报紧急帧（激光落差、超声波极近距）

## 4. 激光测距检测坑/台阶的算法要点

- 安装为**斜向下**，通过安装角与车速计算前探距离：检测距离 ÷ 车速 = 反应时间
  - 例：前探 20cm @ 5km/h ≈ 0.14s 反应窗口 → 建议加大前探距离，或异常即减速
- **特征区分**：
  - 上台阶/路沿：距离值**突变变小**
  - 下坑/落差：距离值**突变变大**
- 做成一对阶跃检测规则（阈值 + 持续时间），触发即上报紧急事件 + 桥端本地限输出

## 5. 物理设计注意

| 事项 | 要求 |
|---|---|
| 线长 | UART 单端信号，**≤50cm**；更长走线换带 RS485 输出的模块或加屏蔽 |
| 传感器供电 | 桥板单独一路 5V（DCDC/LDO），预算 ~300mA（超声波 6×20mA + 激光 2×100mA 量级），**不与电机驱动逻辑电源混用** |
| 干扰隔离 | 传感器电路区与功率区布局分开、分地；探头线尽量短 |
| 安装防护 | 户外送餐车，探头注意防水防尘（IP 等级按模块规格评估，不够加密封结构） |

## 6. 传感器失效与安全的对应关系

| 失效 | 检测手段 | 安全动作 |
|---|---|---|
| 单路超声波失效 | 超时/冻结检测 | 标记无效，降速运行（相邻探头仍有覆盖） |
| 单路激光失效 | 超时/冻结检测 | 限速 + 提示（路面落差检测能力降级） |
| 整桥传感器失联 | 桥端自检上报 | VCU 决策：限速或停车 |
| 全部传感器失效 | 多路同时无效 | 停车（无感知不行驶） |
