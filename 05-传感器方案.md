# 05 传感器方案

> 范围声明：本文件仅覆盖底盘近距感知（超声波 + 激光测距）；Mid-360 与 RTK 惯导属上装感知，不经底盘控制链（见 01 §8）；惯导底盘接口（主干 CAN，10 号矩阵 0x700~0x7DE 预留段，0x7DF 为 UDS 功能寻址，见 10 §2）输入规格待定，已登记见 07。

## 1. 传感器配置与分工

| 传感器 | 数量 | 输出接口 | 安装位置 | 职责 |
|---|---|---|---|---|
| 超声波测距 | **10**（前 4 + 后 4 + 侧向 2） | **UART** | 整车四周（前 4 + 后 4 + 左右各 1） | 近距离避障（~0.2~4m），贴边/低速场景，侧向盲区补盲 |
| 激光测距 | 4 | **UART** | 前 2 + 后 2，**斜向下照射** | 检测前后路面坑洞、台阶落差 |

送餐车特有需求：上人行道、过路沿石 → 激光测距负责提前发现路面落差。

**侧向补盲理由**：4WS 支持原地转向/斜移，此类工况下车体横向外扩，前后向超声波覆盖不到车体侧面——侧向需检测路沿与低矮行人（取餐/行人靠近车侧），故左右两侧各补 1 只超声波。

**接口富余**：每块桥板 16 路 LPUART，现用 6 路（超声波 4 + 激光 2），补侧向后每桥 7 路，余量充足。

**侧向探头安装位置**：🔲 待核——安装高度/角度与车体侧围机械布置联动，机械布置冻结后回填本文档（登记进 07）。

## 2. 接入架构（分布式汇聚到桥端）

传感器跟着物理位置走，分挂前后桥 MCU（S32K344，各 16 路 LPUART 富余）；侧向探头按就近侧分配——左侧挂前桥板、右侧挂后桥板（若机械布置导致走线过长，可整侧对调，见 §5 线长）：

```
前桥板 S32K344                      后桥板 S32K344
├─ LPUART0 ─ 超声波1（前）          ├─ LPUART0 ─ 超声波5（后）
├─ LPUART1 ─ 超声波2（前）          ├─ LPUART1 ─ 超声波6（后）
├─ LPUART2 ─ 超声波3（前）          ├─ LPUART2 ─ 超声波7（后）
├─ LPUART3 ─ 超声波4（前）          ├─ LPUART3 ─ 超声波8（后）
├─ LPUART4 ─ 激光测距·前左          ├─ LPUART4 ─ 激光测距·后左
├─ LPUART5 ─ 激光测距·前右          ├─ LPUART5 ─ 激光测距·后右
└─ LPUART6 ─ 超声波·左侧（补盲）    └─ LPUART6 ─ 超声波·右侧（补盲）
```

**优点**：
- 传感器离采集点最近，UART 走线最短（单端信号怕长距离串扰）
- 负载分摊，VCU 只收打包后的 CAN 报文
- 前桥故障不影响后侧传感器（故障域隔离）

## 3. 软件设计要点（桥端实现）

### 3.1 接收机制

- **DMA + 空闲行中断**接收：不占用 CPU 逐字节中断，DMA 收整帧、空闲中断触发解析
- 每路传感器独立状态机：收帧 → 校验 → 滤波（中值/限幅）→ 更新距离值 + 时间戳

### 3.2 数据有效性规则

1. **超时标无效**：500ms 无新数据 → 状态位置"无效"
2. **无效 ≠ 无障碍**：上报时区分"无效"与"畅通"；避障逻辑对无效值按**降速确认**处理，绝不当作前方畅通
3. **帧校验**：协议有校验位必须验；无校验位做长度 + 范围合理性检查（激光报 0mm 或满量程突变多为乱码，滤掉）
4. **数据冻结检测**：连续多帧完全相同的值（传感器卡死）也要标记异常

### 3.3 上报格式（进 CAN 报文矩阵，10 号）

- 每桥打包上报：**7 个距离值 + 7 个有效标志位**，每桥 **3 帧（超声距离帧+激光距离帧+侧向超声/有效标志帧，见 10 §4）**，20~50Hz
- 异常事件单独上报紧急帧（激光落差、超声波极近距、侧向障碍），紧急帧定义以 10 号矩阵 0x204/0x304 事件报文为准：
  - **触发方向规则**：近距/极近距障碍按**当前运动方向**选择对应侧探头触发——前进时前侧探头触发，**倒车时后侧探头镜像触发**（后侧探头承担倒车方向的近距急停职责）
  - **侧向探头**：任意运动方向均参与触发判定（斜移/原地转向时车体横向外扩）

## 4. 激光测距检测坑/台阶的算法要点

- 安装为**斜向下**，通过安装角与车速计算前探距离：检测距离 ÷ 车速 = 反应窗口
- **特征区分**：
  - 上台阶/路沿：距离值**突变变小**
  - 下坑/落差：距离值**突变变大**
- 做成一对阶跃检测规则（阈值 + 持续时间），触发即上报紧急事件 + 桥端本地限输出

### 4.1 限速档位 × 安装角 × 前探距离 × 端到端延迟联算

端到端延迟构成：传感器帧率 ~50ms（按 20Hz 计）+ 接收/滤波 ~30ms + 阶跃判定 ~20ms ≈ **100ms**（0.1s）。

| 限速档位 | 安装角（斜向下，草案） | 现装前探距离 | 反应窗口 | 端到端延迟 | 所需最小前探距离（0.3s 判定窗口） | 结论 |
|---|---|---|---|---|---|---|
| 5km/h（1.39m/s） | 45° | ~20cm | 0.14s | ~0.1s | ≥0.56m | 现装前探不足，靠低速兜底；建议加大安装高度换前探 |
| 15km/h（4.17m/s） | 45° | ~20cm | 0.05s | ~0.1s | ≥1.67m | 反应窗口已小于端到端延迟，**物理上不可行** |
| 25km/h（6.94m/s） | 45° | ~20cm | 0.03s | ~0.1s | ≥2.78m | 同上，且更严重 |

**结论**：
- 高速档（15/25km/h）必须加大前探距离至**米级**（调整安装角/安装高度），否则执行"**异常即减速**"策略——感知链路延迟决定了高速档不可能靠现有前探距离做有效判定
- **高速档状态机默认"检测异常先减速再确认"**：激光报异常先降到 5km/h 档，确认无落差后再恢复原档位
- **本项列为机械支架设计前必关项**：安装角与安装高度直接决定前探距离，支架设计前必须按本表定稿，并随机械布置项一并向 07 登记

## 5. 物理设计注意（整车供电定案）

| 事项 | 要求 |
|---|---|
| 传感器供电（整车定案） | 整车 **48V→5V 隔离模块（2A/10W）直供**，经各桥板**传感器聚合座**接入（见 09 §1/§3、11 §2.1/§4.5）；**不在桥板本地转换**，原"桥板单独一路 5V"表述作废 |
| 供电预算（10 超声波 + 4 激光重算） | 超声波 10×~20mA ≈ 0.2A + 激光 4×~100mA ≈ 0.4A + 线损/裕量 ≈ **合计 ~1A @5V（~5W）**；2A/10W 容量裕量 ≥100% ✓ |
| 跨域软接 | 传感器在隔离 5V 域，其 GND 经 **1MΩ + 100nF 软接**至车体地——定位为**静电泄放与高频参考，非电位钳位**；两域间共模电压列**实测项**（装机实测后回填）；实测仍有干扰，备选**光耦 / 隔离 UART** 方案 |
| 干扰隔离 | 传感器电路区与功率区布局分开、分地；探头线尽量短 |
| 安装防护 | 户外送餐车，探头注意防水防尘（IP 等级按模块规格评估，不够加密封结构） |

### 5.1 线长核对（UART 单端，原则 ≤50cm）

| 通道 | 预判 | 说明 |
|---|---|---|
| 前 4 / 后 4 超声波 | ≤50cm 可达 | 按就近原则走线 |
| 激光 ×4 | ⚠️ 可能超 50cm | 探头装于车体最前/最后沿，至桥板距离待整车布置核对；超限换带 **RS485 输出**的模块或加屏蔽 |
| 侧向超声波 ×2 | ⚠️ 可能超 50cm | 安装位 🔲 待核（§1）；若至所属桥板超限，可整侧对调挂接或换型 |
| BMS 通信 | 预留行 | 嘉佰达若走 **485/UART 退路**（CAN 协议不可得时的备选，见 10 §4 0x5xx 说明），通道图预留一路至 VCU；长度随整车布置实测后定 |

更长走线统一规则：换带 RS485 输出的模块，或加屏蔽 + 降低波特率。

### 5.2 同排超声波串扰对策

- 同排多只探头同时发射会互相触发（串扰假回波），采用**交替 / 分组触发**：奇偶交替发射，或"前 / 后 / 侧"分组轮询
- 触发时序由桥端统一编排（借 LPUART 命令帧控制发射时机），编排方案随软件设计定稿

## 6. 传感器失效、误报与安全的对应关系

### 6.1 失效与降级表

| 失效 | 检测手段 | 安全动作（量化） |
|---|---|---|
| 单路超声波失效 | 超时/冻结检测 | 标记无效，限速 **15km/h** 运行（相邻探头仍有覆盖） |
| 单路激光失效 | 超时/冻结检测 | 限速 **5km/h** + 提示（路面落差检测能力降级） |
| 侧向超声波失效 | 超时/冻结检测 | 限速 **5km/h**，禁用原地转向/斜移工况（侧向盲区恢复无人看管） |
| 同侧双路失效（前/后 + 侧向同侧） | 多路超时组合判定 | 限速 **5km/h** + 上报；该侧贴边/侧向检测全部失效，禁止贴边工况 |
| 整桥传感器失联 | 桥端自检上报 | VCU 决策：限速 5km/h 就近停车 |
| 全部传感器失效 | 多路同时无效 | 停车（无感知不行驶） |

### 6.2 误报抑制规则

- **多帧确认**：阶跃/近距事件需连续 ≥3 帧（约 150ms）确认才上报紧急帧，单次跳变只进异常统计
- **置信度**：每路维护置信度（多帧一致性 + 幅度合理性）；低置信度事件只做**提示**，不触发急停反射
- 场景对策：
  - **雨滴/溅水**：短窗均值滤波 + 回波幅度门限，抑制单点尖峰
  - **灰尘/污渍糊探头**：持续极近值 + 冻结特征 → 按失效处理（降级表），不当真实障碍反复急停
  - **路面接缝/伸缩缝**：激光阶跃需满足**幅值 + 持续时间**双阈值；接缝窄脉冲不达持续时间，不触发
- 原则：误报宁可降速，漏报绝不放行
