# 08 车身控制器（BCM）方案

> 状态说明：本文档只记录已确定的内容；尚未确定的设计建议在第 7 节列出，确认后再并入正文。

## 1. 需求来源（已确认）

送餐车除行驶底盘外，还需要一个独立控制器负责车身电器与远程控制支持，具体需求：

| # | 需求 | 说明 |
|---|---|---|
| 1 | 上盖开合控制 | 送餐箱体上盖的开/关执行 |
| 2 | 整车远程开机 | 整车支持远程开机，远程开机后进入可远程控制状态 |
| 3 | 5G 路由器管理 | 整车配 5G 路由器，远程控制依赖其在线 |
| 4 | 灯光控制 | 整车灯光的开关控制 |
| 5 | 巡逻模式（扩展） | 警灯 + 控制功放喇叭喊话 |
| 6 | 远程对讲 | 平时远程控制时可对讲对话 |
| 7 | 机械臂预留 | 整车规划机械臂，CAN 等接口需预留（具体方案待定） |
| 8 | 显示屏 | **4.58 寸 TFT 屏幕**，展示取餐二维码与关盖倒计时，不可触控；**STM32 + SPI 裸屏独立显示方案**（见 5.4）；要求不暴露操作系统界面与开机过程 |

## 2. 节点定位（已确定）

- 新增**第四块自研板：车身控制器（BCM）**，独立于底盘控制链（VCU/前后桥）
- 接入**主干 CAN 总线**（500K），与 VCU、前后桥、BMS 同级通信
- 车身功能属安全 **QM 等级**，不参与行驶安全链路；与底盘安全域物理隔离，功能安全论证时独立处理

## 3. 芯片选型（已确定）：NXP S32K312

### 3.1 关键参数（官方核实）

| 参数 | 值 |
|---|---|
| 内核 | 单核 Cortex-M7 @120MHz（ASIL-B，车身 QM 域足够） |
| Flash / RAM | 2MB / 192KB |
| CAN | **6 路 CAN-FD** |
| UART / LIN | 8 路 UART / 8 路 LIN |
| GPIO | 77 |
| 安全/认证 | HSE-B、FOTA、AEC-Q100 Grade 1（-40~125°C） |
| 参考价 | ~$3.2 @1K |
| 交期 | 16 周 |

### 3.2 选择理由

- 与底盘三板**同家族同生态**：工具链（S32DS）、驱动（RTD）、诊断（UDS）框架全部复用，不新增学习成本（曾对比普通 STM32：开发资料多但会引入第二套工具链与分裂的诊断框架，未采用）
- 车规 AEC-Q100，与整车车规目标一致；车身域功能不参与行驶安全，QM 等级，无需锁步核，低成本即可（~$3）
- 外设（6 路 CAN + 大量 GPIO）满足灯光/上盖/功放等开关量与功率驱动扩展，并留有扩展余量（机械臂等后续功能接口预留评估中）

### 3.3 封装料号（已确认）
- **`S32K312NHT0MPBST`**：HDQFP-172（16×16mm，0.65mm 间距）、143 IO、12NC 935436148557，与其他三板封装体系一致（勿买 HDQFP-100 裁剪版 S32K312NHT0MPAST，IO 少）
- 开发板：官方 S32K312EVB-Q172，可用于前期软件验证

## 4. BCM 功能范围（已确定）

| 功能 | 执行内容 |
|---|---|
| 上盖开合 | **伸缩电动推杆**驱动开合 + **电磁锁**锁盖（带锁状态反馈信号） |
| 远程开机 | 整车远程上电流程的执行 |
| 5G 路由器 | 路由器电源管理 |
| 灯光 | 整车灯光开关驱动 |
| 巡逻模式 | 警灯驱动、功放控制 |
| 对讲支持 | 对讲功能的车身侧支持（供电/使能） |
| 显示屏支持 | 屏幕域电源管理（STM32 显示板供电/屏幕背光使能，见 5.4） |
| 舱内环境监测 | 温湿度采集（舱内 + 控制板层两处），超温启动静音风扇（见 5.7） |

## 5. 已确定的设计决策（2026-08-30 确认）

### 5.1 远程唤醒链路（已确定）
- **4G 常电唤醒模块直接接入 BCM（S32K312）**：BCM 低功耗待机监听唤醒指令，收到后执行整车上电时序；具体 4G 模块型号与接口定义待定（见第 7 节）
- 5G 路由器电源由 BCM 管理，上电时序中由 BCM 上电并等待其在线（具体时序待定）
- 背景：5G 路由器关机时无法接收远程指令，必须有常电低功耗通道接收开机指令，否则远程控制存在死锁

### 5.2 功能分工（已确定）
- 对讲/喊话**音频流归上位机**（5G 传输、TTS 合成），BCM 只负责麦克风/功放的供电与使能；警灯为开关量功率输出，由 BCM 驱动（通电自闪型，见 5.5）
- **转向灯/刹车灯：VCU 经 CAN 请求、BCM 驱动功率级点亮**；转向灯为**通电自动闪烁型**（闪烁电路内置于灯具），BCM 只需通断供电，无需 PWM 闪烁逻辑；MCU 芯片不含继电器等功率器件，灯光功率级需外置（器件选型待定，见第 7 节）
- 功率级选型原则（讨论结论，待器件确认）：转向灯/刹车灯等高频负载用车规智能高边开关（禁用继电器）；功放/警灯等低频大功率负载可用继电器；上盖推杆需 H 桥电机驱动（正反转）

### 5.3 上盖机构（已确定硬件）
- 上盖执行器：**伸缩电动推杆**（小型直流推杆，**自带内置限位开关，已确认**）
- 盖锁：**电磁锁，带锁状态反馈信号（无磁信号反馈）**；感性负载，驱动电路必须加续流二极管
- 推杆与电磁锁的联动时序、防夹参数待定（见第 7 节）

### 5.4 显示屏方案（2026-08-30 更新，替代旧决策）
- **现决策：STM32 + SPI 裸屏独立显示**，屏幕定为 **4.58 寸 TFT 屏幕**（用户确认，替代原 6.9 寸尺寸）
- 架构：STM32 显示板经 SPI 驱动条形屏，屏幕域与 Ubuntu 完全隔离——上位机只经 UART 发内容数据（二维码内容、倒计时秒数），不碰显示控制；开机动效由 STM32 上电即播，操作系统界面与开机过程不会暴露（需求背景见 07 决策记录）
- 显示内容：开机动效（存 STM32 Flash）→ 待机品牌页 → 二维码页 → 关盖倒计时页（具体 UI 待定）
- 本决策**替代原"屏幕归上位机、换 HDMI 驱动板版"的旧决策**（旧决策作废）；原"MIPI 屏由上位机驱动"路线终止，**现有 MIPI 屏在本方案下无法使用，需采购 SPI 接口屏幕（4.58 寸 TFT）**（见第 7 节）
- 仍维持的否决项：屏幕数据不得经 VCU 中转（不得穿越行驶安全域）
- STM32 芯片型号：**STM32F407**（LVGL + qrcodegen 有成熟移植）（用户确认）；硬件设计见 `显示板/显示板硬件设计.md`
- 屏幕/背光**不采用延迟上电**（已否决）：STM32 上电即播品牌动效，不会暴露系统画面，无需使能线；待机时由 BCM 显示板电源继电器断电即可（用户确认）

### 5.5 输出级功率器件（已确定）
- 灯光功率级：**Infineon BTS7004-1EPP**（4 通道车规智能高边开关，SPI 诊断，可报开路/短路故障）；转向灯为通电自闪型，只需通断供电，无需 PWM 闪烁逻辑；转向灯等高频负载禁用继电器，功放/警灯等低频大功率负载可用继电器（具体型号待定）
- 推杆驱动：**NXP MC33926**（车规 H 桥，5A，正反转）；H 桥低边串联采样电阻 + 差分放大 → S32K312 ADC，作为防夹电流检测基础（阈值参数待定）
- 警灯：**通电自动闪烁型**（闪烁电路内置于灯具），继电器只负责通电供电，无需软件爆闪逻辑（用户确认）
- 电磁锁：复用高边开关通道 + 续流二极管（感性负载必需）
- 供电架构：灯光/推杆/电磁锁统一走 12V 母线，见 `09-全车供电与线路分配.md`
- **电压已确认：灯具、推杆、电磁锁均为 12V 器件**（用户确认），按 12V 版本采购

### 5.6 4G 唤醒模块（已确定）
- **移远 EG800Z-GL**（Cat.1 **全球版**，2026-08-30 由国内版改定（用户确认），为后续可能销往国外预留）；GNSS 版不需要（定位由上位机负责）
- 购买前需核对目标出口国运营商 LTE 频段是否覆盖（全球版通常也含国内频段，国内可用）；模组级认证（CE/FCC 等）状态按数据手册核查，整车出口需目标国整机准入认证（见 07 待办）
- LGA 封装，打样阶段买带转接板版本；低功耗唤醒（PSM/eDRX、唤醒引脚）按移远官方手册配置（待落地确认）
- 供电：12V→3.8~4.2V 专用 buck + 大电容（搜网峰值 ~2A，禁用 LDO），见 09 文档

### 5.7 舱内温湿度监测与散热（已确定）
- 传感器布置（已确定）：温湿度传感器共 **两颗** —— 一颗装在**舱内**（餐食保温监测），一颗装在**控制板层**（电子器件工作环境监测）；均接 BCM 的 I2C
- 散热逻辑（已确定）：温度超过阈值 → BCM 启动**静音风扇**；风扇经**继电器**驱动（启停由 BCM 控制）
- 数据流：BCM 周期采集（~1Hz）→ CAN 上报 → 上位机记录/展示；超阈值本地报警 + 上报
- 传感器型号：**Sensirion SHT31**（I2C，±0.3°C/±2%RH，舱内款建议带滤帽防护）（已定）
- 报警/风扇启动阈值：待定

## 6. 整车架构更新（已确定部分）

```
                 云端 / 手机远程
                      │
              5G 路由器（远程控制依赖）
                      │
                 上位机（自动驾驶/任务）
                      │
                ┌─────┴─────┐
                │  VCU: 358  │
                └─┬───┬───┬──┘
      主干 CAN 500K│   │   │    │
         ┌───────┘   │   └────┴──────┐
         ▼           ▼              ▼
    前桥MCU(344) 后桥MCU(344)  BMS   BCM: S32K312
                                    ·上盖开合
                                    ·灯光驱动
                                    ·远程开机执行
                                    ·5G路由器电源
                                    ·警灯/功放
```

## 7. 待确认设计建议（未确定，不生效）

以下为讨论中提出、尚未经确认的方案，确认后移入正文并同步更新其他文档：

1. **上盖防夹参数**：基于 MC33926 低边采样电阻采电流，合盖电流超阈即停回退 + 限位开关到位检测 + 超时保护；**阈值待实测后直接写入**（用户确认方式）
2. **推杆与电磁锁联动时序**：解锁（确认反馈）→ 推杆动作 → 限位到位 → 上锁（确认反馈），任一步反馈异常即报故障；具体时序待定
3. **4G 低功耗唤醒配置**：EG800Z 的 PSM/eDRX 模式与唤醒引脚定义，按移远官方手册落地确认
4. **温湿度报警/风扇启动阈值**：阈值待定（传感器型号已定为 Sensirion SHT31，安装位置：舱内 + 控制板层）
5. **显示屏采购确认**：4.58 寸 TFT 屏幕（SPI 接口），型号/驱动 IC 待选；购买时确认与 STM32 的 SPI 驱动兼容
6. **显示板开发方案**：STM32F407 型号已定；与上位机/数据源的 UART 接口定义（内容数据协议）待定；板级设计见 `显示板/显示板硬件设计.md`
