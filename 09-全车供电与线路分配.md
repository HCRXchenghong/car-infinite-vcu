# 09 全车供电与线路分配

> 状态说明：本文档记录已确定的供电框架与决策；标注"待定"的内容确认后再转正。
> 相关文档：电源安全清单见 `04-电源系统（电池与BMS）.md`，CAN 拓扑见 `01-整车电子电气架构.md`。

## 1. 供电层级总览（已确定框架）

```
电池 48V 76Ah（BMS 嘉佰达）
  └─ 总空开（1 只，用户确认）
       └─ 纯铜分线器（集中配电点）
            ├─ 大电流支路：驱动伺服控制器 ×2（MSSD）
            ├─ 大电流支路：转向关节电机 ×4（达妙，48V 直供，不经桥板）；机械臂关节电机（达妙，后期）也归本类支路
            └─ 低压电源系统：
                 ├─ 48V→12V 无隔离主模块（50A/≈600W）→ 12V 母线
                 │    ├─ 控制板 12V 输入：VCU(358)、前桥板(344)、后桥板(344)、BCM(312)（BMS 逻辑为包内取电，以厂家手册为准）
                 │    ├─ 设备 12V：上位机、5G 路由器、显示板、灯光等（见下表）
                 │    └─ 执行器 12V：上盖推杆、电磁锁等（见下表）
                 │         └─ 板级二次降压：12V→5V→3.3V（各控制板内，板载自供）
                 ├─ 48V→5V 隔离 DCDC（2A/10W）→ 传感器专用供电（超声波×10 含侧向×2/激光×4，经聚合座直供；温湿度为 BCM 板载（I2C），不经本路（见 11 §2）；预算复核：05 已按 10 只重算合计 ≈1A，仍满足 2A/10W）
                 ├─ 48V→12V 隔离（感知域专用，4A/50W）→ Mid-360（10W）+ RTK 惯导（15W）（仅由本模块供电，不计入主模块预算，与底盘功率域隔离）
                 └─ 48V→24V 预留（5A/120W）→ 用途待定
```

**原则（已定，三域隔离）：集中配电（空开 + 纯铜分线器）；不隔离的归一个大的（12V 主模块 50A/600W，连续 ≥30A，供控制板/灯光/执行器/设备）；隔离的分域供给——弱信号传感域（隔离 5V）与感知设备域（隔离 12V/50W，Mid-360+惯导）；各板芯片 5V/3.3V/1.2V 板载自供；分级保险。**注：Mid-360（10W）与 RTK 惯导（15W）仅由隔离 12V/50W 感知域模块供电，不计入主模块预算。功率定案见 `11-整车线束与连接器方案.md` §2.1。

## 2. 各节点供电表（已确定）

| 节点 | 输入 | 板级/说明 |
|---|---|---|
| VCU（S32K358） | 12V | 板内 12V→5V→3.3V（核心电压按数据手册） |
| 前后桥板（S32K344）×2 | 12V | 同上；传感器供电来自整车隔离 5V（隔离 DCDC 供给，不经本板转换）；负载为超声波 ×5（含侧向×1）+ 激光 ×2 每桥 7 传感器（见 05/11 §4.5） |
| BCM（S32K312） | 12V | 板内 12V→5V→3.3V；**4G 模块单独供电**：12V→4V buck（≥2A，建议 ≥3A，待机域独立）+ 大电容（搜网瞬间峰值 ~2A，禁用 LDO）；4G 为常电（待机守夜） |
| BMS（嘉佰达） | 电池内部 | 包内取电（以厂家手册为准），不由控制板 12V 供电 |
| 上位机（临滴科技，机型待定，暂按 RK3588 评估） | 12V（该板供电规格待确认） | 远程开机时序中由 BCM 上电 |
| 上位机感知/定位（Mid-360 + RTK 融合设备） | **隔离 12V**（感知域隔离模块，48V→12V 50W） | Mid-360（10W）与 RTK 惯导（15W）仅由本模块供电，不计入主模块预算；Mid-360 经 100M 以太网接上位机；RTK 设备接口按选型定（见 01 §8）；不入底盘控制链；与底盘功率域地电位隔离 |
| 5G 路由器 | 12V（常见规格，按实物确认） | 电源由 BCM 管理（远程开机时序） |
| 显示屏（SPI 4.58 寸 TFT + STM32 显示板） | 12V | STM32 显示板 12V 供电，待机由 BCM 继电器断电；不设延迟上电使能线（STM32 上电即播动效，已否决）；内容数据来自上位机（UART） |
| 灯光（清单已定，见 BCM板 文档） | 12V 母线 → BTS7040-2EPA×4 + BTS7200-4EPA×3 高边（7 片 20 通道，见 08 §5.5）→ 各灯 | 转向灯通电自闪型；转向/刹车灯由 VCU 经 CAN 请求；警灯 ×2 高边直驱（U2） |
| 上盖推杆（12V 版，带内置限位开关） | 12V 母线 → MC33926 H 桥 | 防夹电流检测优先用 MC33926 FB 脚接 ADC（自带比例反馈） |
| 电磁锁 | 12V → 高边开关 U4 通道 + 续流二极管 | 带锁状态反馈（无磁信号） |
| 功放 | 12V → 继电器主电；音频来自上位机 | 使能由 BCM 控制 |
| 静音风扇 | 12V → 继电器 → 风扇 | 启停由 BCM 控制（温湿度超阈触发，见 08 §5.7） |
| 警灯 ×2 | 12V → 高边开关（U2，见 08 §5.5） | 通电自动闪烁型（闪烁电路内置于灯具），高边开关只通电供电（改高边直驱，不再走继电器） |
| 摄像头组（第一版 AHD，后续迁 GMSL） | 12V 支路（BCM 继电器） | 待机断电；同轴线束预留（AHD/GMSL 同轴兼容，迁移不换线）；数据经同轴+解串器板进上位机，不经底盘 CAN（见 01 §8） |

## 3. DCDC 采购清单（初版）

| 位置 | 规格 | 数量 | 状态 |
|---|---|---|---|
| 48V→12V 无隔离主模块 | **50A / ≈600W**（连续 ≥30A），输入 48V 系 | 1 | 具体型号待定；预算含控制板/灯光/上装灯/开盖电机/上位机/机械臂控制器等（Mid-360/RTK 惯导由感知域隔离模块供电，不计入），见 11 §2.1 |
| 48V→5V 隔离（传感器专用） | **2A / 10W** | 1 | 具体型号待定 |
| 48V→12V 隔离（感知域专用） | **4A / 50W** | 1 | Mid-360 + RTK 惯导；具体型号待定 |
| 48V→24V 预留 | **5A / 120W** | 1 | 用途待定，是否隔离待确认 |
| 板内 12V→5V | buck ≥2A | ×4（VCU/前桥/后桥/BCM） | 待定 |
| 5V→3.3V | LDO | 每板 | 常规料 |
| 4G 供电 | 12V→4V buck ≥2A（建议 ≥3A，待机域独立）+ 大容量电容 | 1 | 待定 |
| 高边开关（灯光/执行器功率级） | BTS7040-2EPA（2 通道，3.5A/通道） | ×4 | 定案（立创 C534839）；通道分配见 08 §5.5 |
| 高边开关（灯光/执行器功率级） | BTS7200-4EPA（4 通道，1A/通道） | ×3 | 定案（立创 C5359853）；通道分配见 08 §5.5 |

**选购五参数**：输入电压范围 / 输出电压与电流 / 效率 / EN 使能脚（上电时序必需）/ 工作温度（-40℃ 起）。

**保护**：各 12V 支路串保险；BCM 功率级输入独立保险 50A（定案见 11 §4.3：受控负载同时峰值约 40A）；48V 与 12V 母线入口 TVS；电源入口防反接。

## 4. 隔离问题说明（已确定原则）

- **隔离策略（2026-08-30 升级定稿，三域隔离）**：弱信号传感域走隔离 5V；感知设备域（Mid-360+惯导）走隔离 12V/50W；主干 CAN 四块 S32K3 控制板（VCU/前桥/后桥/BCM）用隔离收发器（待选型，ISO1042 类）；其余由无隔离大模块（48V→12V 50A/600W，连续 ≥30A）供；板内芯片电源全部板载自供；机械臂关节电机（达妙）为 48V 供电，走分线器大电流支路，不计入 DCDC
- 三个可能涉及"隔离"的场景及其解法：
  1. **CAN 总线（车规升级，2026-08-30）**：四块 S32K3 控制板（VCU/前桥/后桥/BCM）主干 CAN 采用**隔离 CAN 收发器**（待选型，ISO1042 类；桥板横跨功率区，防地弹拖垮总线）；BMS 侧维持原口；所有 CAN 口共模电感 + TVS 基础防护；桥内 CAN（电机侧不可改，桥内收发器用 TJA1443）桥板端加共模电感 + TVS（详见 11 §3.1）
  2. **模拟弱信号**（防夹电流采样）：本地低边采样 + 差分放大，不做电源隔离
  3. **跨板长线供电**：传感器由整车隔离 5V/2A/10W 直供（经聚合座），不在桥板就近取电；温湿度传感器为 BCM 板载（I2C），不经传感器线束
  4. **激光雷达（Mid-360）与 RTK 惯导：隔离供电（2026-08-30 升级）**：虽然二者为智能数字设备，但按车规级现场可靠性要求（防电机 PWM 噪声/地弹/现场瞬态），划入独立感知隔离域：48V→12V 隔离模块 50W 供电，与底盘功率域地电位隔离；后手：实测仍有干扰再加板级滤波/隔离 CAN 收发器
- 结论：三域隔离——弱信号传感域（隔离 5V/10W）+ 感知设备域（隔离 12V/50W，Mid-360+惯导）+ 主干 CAN 隔离收发器（见 11 §3.1）；不隔离的归一个大的（12V 主模块 50A/600W，连续 ≥30A）；24V 预留 120W 用途待定

## 5. CAN 物理层布线（已确定通用做法）

- 双绞线；主干 500K（VCU—前后桥—BMS—BCM）、桥内转向 1Mbps、桥内驱动（速率待厂家）
- 每段总线两端各 120Ω 终端电阻；分支短线化，避免长星型拓扑
- 线束与功率线（电机相线）分槽走线
- 屏蔽层单点接地**已定：VCU 端单点接地**（见 11 §3.1）

## 6. 本轮已确定决策

| 决策 | 结论 |
|---|---|
| 4G 唤醒模块 | 移远 **EG800Z-GL**（Cat.1 全球版，为后续可能销往国外预留）；GNSS 版不需要（定位由上位机负责）；LGA 封装，打样策略：先转接板验证 → 量产裸模组上板；低功耗唤醒（PSM/eDRX、唤醒引脚）按官方手册配置；购买前核对目标出口国频段 |
| 灯光功率级 | Infineon PROFET+2：**BTS7040-2EPA ×4 + BTS7200-4EPA ×3（7 片 20 通道）**，AEC-Q100 Grade 1，SPI 菊花链诊断；通道分配见 08 §5.5；警灯 ×2 改高边直驱，继电器收敛为 6 只 |
| 推杆驱动 | NXP **MC33926**（车规 H 桥，5A 峰值，连续电流取决于散热；推杆堵转电流需 <6.5A 限流点）+ FB 脚电流反馈（防夹检测优先用 FB 接 ADC） |
| 屏幕落地方案 | **STM32 + SPI 裸屏独立显示**（屏幕定为 4.58 寸 TFT），上位机只经 UART 发内容数据；本决策替代此前"换 HDMI 驱动板 + 上位机驱动"的旧决策（作废）；维持否决"屏幕数据经 VCU 中转" |
| 推杆限位 | 推杆**自带内置限位开关**（已确认） |
| BCM 板输入电压 | 12V（板内降至 5V/3.3V） |
| 供电隔离原则 | 隔离的单独一个（传感器 5V），不隔离的归一个大的（12V 主模块）；板内电源板载自供 | 见 11 §2/§2.1 |
| 电源分配（2026-08-30 更新） | 总空开 → 纯铜分线器 → 伺服×2/转向电机×4（+后期臂关节电机）大电流支路（每路加分路熔断）+ 12V 无隔离主模块（50A/600W，连续 ≥30A，预算含上位机/臂控/上装灯/开盖电机等；Mid-360/惯导归感知域隔离模块）+ 隔离 5V（10W 传感器专用）+ 24V 预留（120W） | 见 09 §1 与 11 §2.1 |
| 12V 器件确认 | 灯具、推杆、电磁锁均为 12V 器件 | 统一走 12V 母线（用户确认） |
| 警灯类型 | 通电自动闪烁型（闪烁电路内置于灯具） | 高边开关（U2）只通电供电，无需软件爆闪逻辑（不再走继电器） |

## 7. 待定清单

| # | 事项 | 说明 |
|---|---|---|
| 1 | ~~灯具/推杆统一 12V~~ | 已定：均为 12V 器件（用户确认），本项关闭 |
| 2 | ~~转向关节电机供电规格~~ | 已确认达妙供电电压 24~48V，本项关闭 |
| 3 | 各负载实际功耗核算 | 据此定稿主 DCDC 电流规格（主模块规格已定 50A/600W，连续 ≥30A） |
| 4 | DCDC 具体型号/模块选型 | 按第 3 节清单逐项定料 |
| 5 | 上位机机型与供电规格 | 机型待定，暂按临滴 RK3588 评估；确认 12V 输入兼容性 |
| 6 | 推杆与电磁锁联动时序 | 见 `08-车身控制器（BCM）方案.md` 第 7 节 |
| 7 | 防夹电流阈值参数 | 实测推杆空载/夹持电流后标定 |
| 8 | 隔离 CAN 收发器选型 | 主干 CAN 四板用，待选型（ISO1042 类）；桥内用 TJA1443 |
| 9 | 48V 大电流支路分路熔断 | 伺服/转向电机等大电流支路每路加分路熔断，熔断规格待定 |

## 8. 负载清单与上电时序（新增）

### 8.1 常电负载清单（待机不断电）

| 负载 | 供电路径 | 备注 |
|---|---|---|
| 4G 唤醒模块（EG800Z-GL） | 12V 母线 → 4V buck（待机域独立） | 低功耗待机（PSM/eDRX） |
| BCM MCU（S32K312）待机 | 12V 母线 → 板内电源 | 低功耗监听唤醒 |
| 12V 主模块空载损耗 | 48V → 12V 主模块 | 待机时仅带上述负载 |

> 注：待机功耗预算待实测后锁定。

### 8.2 受控支路清单（开关器件归属）

**继电器 ×6**（主电通断，由 BCM 控制）：

| # | 受控负载 |
|---|---|
| 1 | 5G 路由器 |
| 2 | 上位机 |
| 3 | 显示板（STM32 + 屏） |
| 4 | 静音风扇 |
| 5 | 功放 |
| 6 | 摄像头组 |

**高边开关 7 片 20 通道**（BTS7040-2EPA ×4 + BTS7200-4EPA ×3，负载分配与通道明细见 08 §5.5）：近光/远光/雾灯/警灯×2/上装灯/机械臂控制器 12V/电磁锁/4 路转向/刹车×2/示廓/倒车/其余小功率灯光×3 + 1 路冗余。

### 8.3 上电时序步骤表（BCM 主导低压时序）

| 步骤 | 动作 | 条件/判据 | 超时处理 |
|---|---|---|---|
| 1 | 4G 模块常电待机 → 收到远程唤醒指令 | 唤醒指令校验通过 | 无效指令不响应，继续待机 |
| 2 | BCM 自启，闭合 5G 路由器继电器 | 路由器联网/在线确认 | 超时重试后上报故障 |
| 3 | 闭合摄像头组继电器（随第 1 级继电器并联合闸：与步骤 2 同级，可合并执行；本表按实际时序单列） | 摄像头组上电（待机断电负载，见 §2） | 超时上报 |
| 4 | 闭合上位机继电器 | 上位机启动完成（心跳/UART 就绪） | 超时上报 |
| 5 | 闭合显示板继电器 | 显示板启动（上电即播动效） | — |
| 6 | 通知 VCU 上电，等待 **VCU_Ready 握手**（0x110，PowerSeqStep 已删除） | VCU_Ready 置位 | 超时上报 |
| 7 | VCU 接管**高压预充/主接触器**时序 | 预充完成、主接触器闭合 | 由 VCU 按功能安全策略处理 |
| 8 | 前/后桥板上电 | 主干 CAN 节点在线 | 超时上报 |
| 9 | 时序状态经 0x400 **PowerSeqState 由 BCM 上报** | — | — |
