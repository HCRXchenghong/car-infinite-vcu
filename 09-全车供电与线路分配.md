# 09 全车供电与线路分配

> 状态说明：本文档记录已确定的供电框架与决策；标注"待定"的内容确认后再转正。
> 相关文档：电源安全清单见 `04-电源系统（电池与BMS）.md`，CAN 拓扑见 `01-整车电子电气架构.md`。

## 1. 供电层级总览（已确定框架）

```
电池 48V 76Ah（BMS 嘉佰达）
  └─ 总空开（1 只，用户确认）
       └─ 纯铜分线器（集中配电点）
            ├─ 大电流支路：驱动伺服控制器 ×2（MSSD）
            ├─ 大电流支路：转向关节电机 ×4（达妙，48V 直供，不经桥板）
            └─ 两路 → 隔离 DC 转换器（用户确认）
                 ├─ 24V 输出（负载清单待定）
                 └─ 12V 输出 → 12V 母线
                      ├─ 控制板 12V 输入：VCU(358)、前桥板(344)、后桥板(344)、BCM(312)、BMS 逻辑
                      ├─ 设备 12V：上位机、5G 路由器、显示板、灯光等（见下表）
                      └─ 执行器 12V：上盖推杆、电磁锁等（见下表）
                           └─ 板级二次降压：12V→5V→3.3V（各控制板内）
```

**原则：集中配电（空开 + 纯铜分线器）、低压经隔离 DC 转换器、分级保险；5V 分配方式（转换器输出或板级降压）待定。**

## 2. 各节点供电表（已确定）

| 节点 | 输入 | 板级/说明 |
|---|---|---|
| VCU（S32K358） | 12V | 板内 12V→5V→3.3V（核心电压按数据手册） |
| 前后桥板（S32K344）×2 | 12V | 同上；传感器 5V 独立一路（每桥 ~300mA，与驱动逻辑电源分开） |
| BCM（S32K312） | 12V | 板内 12V→5V→3.3V；**4G 模块单独供电**：12V→3.8~4.2V buck + 大电容（搜网瞬间峰值 ~2A，禁用 LDO）；4G 为常电（待机守夜） |
| BMS（嘉佰达） | 电池内部 | — |
| 上位机（临滴科技 RK3588 板） | 12V（该板供电规格待确认） | 远程开机时序中由 BCM 上电 |
| 上位机感知/定位（Mid-360 + RTK 融合设备） | 12V（按设备规格） | Mid-360 经 100M 以太网接上位机；RTK 设备接口按选型定（见 01 §8）；不入底盘控制链 |
| 5G 路由器 | 12V（常见规格，按实物确认） | 电源由 BCM 管理（远程开机时序） |
| 显示屏（SPI 4.58 寸 TFT + STM32 显示板） | 12V | STM32 显示板 12V 供电，待机由 BCM 继电器断电；不设延迟上电使能线（STM32 上电即播动效，已否决）；内容数据来自上位机（UART） |
| 灯光（清单已定，见 BCM板 文档） | 12V 母线 → BTS7004-1EPP ×4 高边 → 各灯 | 转向灯通电自闪型；转向/刹车灯由 VCU 经 CAN 请求；警灯 ×2 走继电器 |
| 上盖推杆（12V 版，带内置限位开关） | 12V 母线 → MC33926 H 桥 | H 桥低边加采样电阻做防夹电流检测 |
| 电磁锁 | 12V → 高边开关通道 + 续流二极管 | 带锁状态反馈（无磁信号） |
| 功放 | 12V → 继电器主电；音频来自上位机 | 使能由 BCM 控制 |
| 静音风扇 | 12V → 继电器 → 风扇 | 启停由 BCM 控制（温湿度超阈触发，见 08 §5.7） |
| 警灯 | 12V → 继电器 | 通电自动闪烁型（闪烁电路内置于灯具），继电器只通电供电 |
| 摄像头组（第一版 AHD，后续迁 GMSL） | 12V 支路（BCM 继电器） | 待机断电；同轴线束预留（AHD/GMSL 同轴兼容，迁移不换线）；数据经同轴+解串器板进上位机，不经底盘 CAN（见 01 §8） |

## 3. DCDC 采购清单（初版）

| 位置 | 规格 | 数量 | 状态 |
|---|---|---|---|
| 48V→24V / 48V→12V 隔离 DC 转换器 | 隔离型，输入 48V 系；输出电压/功率按负载核算后定（24V 负载清单待定，5V 分配方式待定） | 2 路 | 具体型号/模块待定 |
| 板内 12V→5V | buck ≥2A | ≥3（VCU/桥板/BCM） | 待定 |
| 5V→3.3V | LDO | 每板 | 常规料 |
| 4G 供电 | 12V→3.8~4.2V buck ≥2A + 大容量电容 | 1 | 待定 |

**选购五参数**：输入电压范围 / 输出电压与电流 / 效率 / EN 使能脚（上电时序必需）/ 工作温度（-40℃ 起）。

**保护**：各 12V 支路串保险；48V 与 12V 母线入口 TVS；电源入口防反接。

## 4. 隔离问题说明（已确定原则）

- **低压系统经隔离 DC 转换器取得**（用户确认，替代原"非隔离主 DCDC"方案）；整车共地方式随转换器接地方案定（待确认）
- 三个可能涉及"隔离"的场景及其解法：
  1. **CAN 总线干扰**：先不隔离（差分信号本身抗扰），实测有问题再加隔离 CAN 收发器（如 ISO1042 类）
  2. **模拟弱信号**（防夹电流采样）：本地低边采样 + 差分放大，不做电源隔离
  3. **跨板长线供电**：传感器在桥板就近取电（线短），无需隔离
- 结论：低压系统采用**隔离 DC 转换器**（2026-08-30 用户确认，替代此前"不采购隔离型"结论）

## 5. CAN 物理层布线（已确定通用做法）

- 双绞线；主干 500K（VCU—前后桥—BMS—BCM）、桥内转向 1Mbps、桥内驱动（速率待厂家）
- 每段总线两端各 120Ω 终端电阻；分支短线化，避免长星型拓扑
- 线束与功率线（电机相线）分槽走线
- 屏蔽层单点接地做法待定

## 6. 本轮已确定决策

| 决策 | 结论 |
|---|---|
| 4G 唤醒模块 | 移远 **EG800Z-GL**（Cat.1 全球版，为后续可能销往国外预留）；GNSS 版不需要（定位由上位机负责）；LGA 封装，打样阶段买带转接板版本；低功耗唤醒（PSM/eDRX、唤醒引脚）按官方手册配置；购买前核对目标出口国频段 |
| 灯光功率级 | Infineon **BTS7004-1EPP**（4 通道车规智能高边，SPI 诊断；转向灯通电自闪只需通断） |
| 推杆驱动 | NXP **MC33926**（车规 H 桥 5A）+ 低边采样电阻（防夹电流检测） |
| 屏幕落地方案 | **STM32 + SPI 裸屏独立显示**（屏幕定为 4.58 寸 TFT），上位机只经 UART 发内容数据；本决策替代此前"换 HDMI 驱动板 + 上位机驱动"的旧决策（作废）；维持否决"屏幕数据经 VCU 中转" |
| 推杆限位 | 推杆**自带内置限位开关**（已确认） |
| BCM 板输入电压 | 12V（板内降至 5V/3.3V） |
| 供电隔离原则 | 低压经隔离 DC 转换器（用户确认，替代原非隔离方案） | 共地方式随转换器接地方案定 |
| 电源分配（2026-08-30 更新） | 总空开 → 纯铜分线器 → 伺服×2/转向电机×4 大电流支路 + 两路隔离 DC 转换器（24V/12V/5V） | 见第 1 节；24V 负载清单与转换器型号待定 |
| 12V 器件确认 | 灯具、推杆、电磁锁均为 12V 器件 | 统一走 12V 母线（用户确认） |
| 警灯类型 | 通电自动闪烁型（闪烁电路内置于灯具） | 继电器只通电供电，无需软件爆闪逻辑 |

## 7. 待定清单

| # | 事项 | 说明 |
|---|---|---|
| 1 | 灯具/推杆统一 12V | 建议统一走 12V 母线（购买时确认 12V 版灯具与 12V 版推杆）；确认后原"额定电压确认"事项关闭 |
| 2 | 转向关节电机供电规格 | 按达妙 J10010 手册确认 48V 直供参数 |
| 3 | 各负载实际功耗核算 | 据此定稿主 DCDC 电流规格（当前按 ≥15A 预留） |
| 4 | DCDC 具体型号/模块选型 | 按第 3 节清单逐项定料 |
| 5 | 上位机机型与供电规格 | 确认 12V 输入兼容性 |
| 6 | 推杆与电磁锁联动时序 | 见 `08-车身控制器（BCM）方案.md` 第 7 节 |
| 7 | 防夹电流阈值参数 | 实测推杆空载/夹持电流后标定 |
