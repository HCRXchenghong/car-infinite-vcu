# 03 执行器配置（转向与驱动）

## 1. 转向系统：达妙科技关节电机

### 1.1 配置

- **型号：DM-J10010-2EC × 4**（每轮一个转向关节）
- 安装方式：四轮四转，电机输出轴直接（或经短传动）驱动车轮转向轴

### 1.2 关键参数（官方核实）

| 参数 | 值 |
|---|---|
| 额定扭矩 | **40 Nm** |
| 峰值扭矩 | **150 Nm** |
| 编码器 | 磁性双编码器（绝对值，断电不丢位置） |
| 通信 | CAN / CAN-FD，**1Mbps** |
| 控制模式 | MIT 模式（位置/速度/力矩+Kp/Kd）、位置模式、速度模式 |
| 供电电压 | **24~48V**（已确认，用户确认；48V 直供可行，07/09 中该待定项经此确认关闭） |
| 官方资料 | Gitee 仓库：`gitee.com/kit-miao/damiao` |

> 协议细节（报文格式/参数表）待厂家手册，官方 Gitee 仓库资料为主要参考。

**仓库关键目录**：
- `0. 固件`：全型号电机固件
- `1. 关节电机`：各型号说明书/图纸/曲线
- `5. 控制例程`：STM32 / C++ / Python / Matlab / ROS（**重点参考 STM32 例程，移植到 S32K3**）
- `9. 使用手册`：`电机上手流程(三)-MIT模式说明`、`(四)-CANID与MasterID的配置建议` 必读
- `B. Canopen`：达妙电机亦支持 CANopen（可选路线）

### 1.3 协议要点

- 总线速率固定 **1Mbps**（与 500K 总线不能混挂）
- **CAN ID + Master ID 机制**：4 个转向电机配 4 组不同 ID，桥端 MCU 作为 Master
- 控制帧与状态帧成对：桥端发控制帧，电机回状态帧（含位置、速度、温度、故障）
- 转向建议用**位置模式 + 速度/力矩限幅**（比纯力矩模式安全可控）

### 1.4 必配安全项（待执行）

| # | 事项 | 要求 |
|---|---|---|
| 1 | **软件限扭矩** | 在达妙上位机 + 控制指令中把 TMAX 限制到机械结构安全值的 60~70%（150Nm 峰值足以打坏拉杆伤到人，这是硬要求） |
| 2 | **失联行为确认** | 用上位机确认丢帧后行为，转向理想失效姿态 = 保持当前位置、输出维持力矩（不自由甩动、不猛回中）；固件不支持则机械加自锁/阻尼 |
| 3 | **机械硬限位** | 转向必须设机械限位，防止软件失控时硬拧损坏减速器 |
| 4 | **零位标定流程** | 绝对编码器装车后需标定"机械中位 ↔ 编码器读数"映射；建议设计装配标定流程（转到左/右硬限位取中），结果存 VCU 标定区 |
| 5 | **温度监控** | 状态帧温度字段超阈值降输出/报警；安装位置保证通风 |
| 6 | **选型余量** | 持续负载按最大扭矩 30~40% 运行，户外连续运行留温升余量 |

### 1.5 转向负载量级验算（占位，待实测修正）

- 按满载单轮轴荷 60~100kg、主销偏距 20~30mm 估算：原地转向需求约 **12~30Nm**
- 40Nm 额定扭矩有余量；限扭目标（见 §1.4 第 1 条）的具体 Nm 值**待机械核算后定**
- 以上数值均为估算，待实测修正

---

## 2. 驱动系统：轮毂电机 + 一拖二伺服控制器

### 2.1 配置

- **轮毂电机：48V 500W 编码轮式霍尔无刷电机 × 4**，最高车速 25 km/h
- **伺服控制器：汕头 MSEAG `MSSD-100EMA_2EC_N` 一拖二伺服控制器 × 2**（每台独立控制 2 个轮毂电机）

### 2.2 MSSD-100EMA_2EC_N 关键参数（彩页核实）

| 参数 | 值 |
|---|---|
| 供电输入 | DC 10~56V（峰值 9~59V） |
| 长期输出电流 | 40A/台（手册建议三轮车应用 ≤25~30A） |
| 最大输出电流 | 100A/台 |
| 最大输出电流设置范围 | 0.5~100A |
| 过流保护电流 | 1~180A 可设（建议设为额定 3~4 倍） |
| 电流测量分辨率 | 120mA |
| 堵转（扭矩）保护时间 | 1ms~65535ms 或 0（关闭） |
| 控制方式 | 仅 FOC；双电机独立/同步/**差动**控制 |
| 通信 | CAN / 485 / TTL，MODBUS-RTU；485 波特率 9600~115200 |
| 霍尔输出 | 5V / 200mA |
| 刹车端口 | 48V / 2A |

### 2.3 兼容性核对结论

| 项目 | 结论 |
|---|---|
| 电压匹配 | ⚠️ 16 串 LFP 满电 54.4~57.6V 贴近 56V 上限 → **充电截止电压设 54.4V（3.4V/串）**，并评估再生制动电压尖峰 |
| 持续电流 | ✅ 每台带 2×500W（额定 20.8A）在 40A 内 |
| 峰值叠加 | ⚠️ 两台硬件最大 200A > BMS 峰值 180A → 按电机额定约束反推输出电流设定（见 §2.4），实际设定值远低于硬件上限 |
| 差动模式 | ✅ 可配合四轮转向做原地转向辅助（后续功能） |
| CAN 协议 | **CAN 2.0，类 CANopen**：SDO 参数配置 + 电机状态主动上报（用户转述信息）；⚠️ 波特率与确切报文格式仍需厂家手册确认 |

**高 SOC 再生限制策略**（再生制动回充会抬高母线电压，高 SOC 时电池受电能力差、电压抬升更显著）：

- SOC ≥95% 或母线电压 ≥55.5V 时：**禁止再生回充**（或大幅降低回充强度），制动需求由限速/机械制动兜底
- 评估母线泄放（耗能）电阻的必要性以吸收再生尖峰，**列入实测项**
- 再生尖峰量级参考：25 km/h 四轮同时再生制动时，回充功率量级约 **1kW**

### 2.4 必配参数（按官方规格表重写，对着手册逐条设置）

> **设定原则：先定电机侧约束，再反推驱动器设置。** 本项目轮毂电机为 500W 级（额定电流约 10~12A/台，两台合计额定约 20.8A）：最大输出电流设置不得高于电机自身额定电流约束；过流跳闸电流按电机额定电流 3~4 倍设定。过流保护必须与工作上限自洽——正常运行达不到跳闸值，跳闸只作故障兜底（堵转/短路/失控），避免"工作上限高于保护阈值"的逻辑倒挂。

| 参数 | 物理域 | 官方规格值 | 本项目推荐设定 | 设定逻辑 |
|---|---|---|---|---|
| 电源输入电压 | 母线输入 | 额定 DC 10~56V；极限 9~59V | 48V 系统，充电截止 54.4V，处于额定范围 | 再生尖峰对策见 §2.3 高 SOC 再生限制策略 |
| 长时间输出电流 | 输出侧（双电机合计） | 40A/台 | 每台 2×500W 额定 ≈20.8A，在 40A 内 | 固有指标，无需设定 |
| 最大输出电流设置 | 输出侧 | 可设 0.5~100A（硬件最大 100A） | **≤ 电机额定电流（约 12A/台）** | 按电机额定约束设定，不得高于电机承受能力 |
| 电机锁相电流 | 相电流 | 0、0.5A~最大电流 | 按保持需求设定，≤ 电机额定 | 驻车保持工况用 |
| 电机运行相电流 | 相电流 | 测量分辨率 120mA | 额定 ≈10~12A/台（500W 级） | 电机本体保护与监控基准 |
| 过流关闭电流（跳闸） | 输出侧（保护） | 可设 1~180A，一般为额定电流 3~4 倍 | **≈36~48A**（按电机额定 ~12A 的 3~4 倍） | 与最大输出设定自洽：工作上限 ≪ 跳闸值，跳闸为故障兜底 |
| 堵转（扭矩）保护时间 | 输出侧（保护） | 1ms~65535ms 或 0（不保护） | 500~1000ms（禁止设 0） | 送餐车卡路沿/门槛高频，防烧电机 |
| 通讯断线保护 | 通信保护 | 485/TTL：1s~65535s 或 0（不保护） | **必须开启，目标 200~500ms** | 失联即停输出，安全底线；需确认 CAN 模式下同样生效。⚠️ 官方规格表载 1s~65535s，疑为 ms 单位笔误（同表堵转保护为 ms 量级），实际可调范围以协议手册核实为准；若下限确为 1s，则失联兜底由桥端 100~200ms 心跳监测承担（见 05/06） |
| 抱闸接口 | 输出侧（辅助供电） | 48V/2A | 能力参考；实际抱闸供电按 §3 约束走独立常电支路 | 与 OUT 接口相互独立 |
| OUT 接口 | 输出侧（辅助供电） | 48V/2A | 预留 | — |
| 5V 电源 | 输出侧（辅助供电） | 最大 200mA | 供霍尔传感器 | 注意 200mA 为总额定，多路霍尔需核算预算 |
| CAN 节点地址 | 通信 | 1~255（0 为广播地址） | 两台驱动器设不同地址 | 避免地址冲突 |
| CAN 通讯波特率 | 通信 | 数据手册载"9600~115200bps"，⚠️ 疑为笔误（CAN 物理层无此量纲，该区间系 485/TTL 特征） | **以协议手册为准**，待厂家确认 | 不得以此为设定依据 |

### 2.5 驱动负载量级验算（占位，待实测修正）

- 按轮径 300mm 估算：25 km/h 需轮速约 **440 rpm**（估算，待实测修正）
- 按满载总重 150kg 估算：爬坡能力约 **6~8%**（估算，待实测修正）
- 以上数值均为估算，待实测修正；电机极对数/KV 选型需结合 §2.4 闭环速度设定范围核对（110000/极对数）

---

## 3. 机械制动（兜底，待选型）

- **驱动轮独立机械抱闸**：不依赖任何电子系统的最终安全制动，**断电抱死**
- **供电约束**：抱闸由独立常电支路供电，供电路径独立于急停切断回路（急停切断驱动使能/供电后，抱闸按断电抱死逻辑动作，不受驱动回路状态牵连；与 04 §4.2 急停链路定案一致）
- 这是非车规成品控制器（达妙/伺服）能通过车规安全论证的关键兜底
- 安装可行性：轮毂内部空间有限，抱闸能否装入轮毂内部 **🔲 待机械评估**
- 状态：🔲 待选型/安装设计

## 4. 限速合规说明

- 25 km/h 是电动自行车国标上限；无人配送车在人行道/非机动车道试点地区常限速 8~15 km/h
- **软件实现可配置限速档位**（园区 8 / 非机动车道 15 / 测试场 25），标定参数切换，不硬编码
