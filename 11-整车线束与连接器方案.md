# 11 整车线束与连接器方案

> 状态：已定原则 + 草案细节。按用户要求：**本文档不定任何具体距离/长度**，只定拓扑、线型、连接方式与预留。
> 连接器体系：车规多芯防水 ECU 连接器（参考规格：TE AMP 系 23 孔插头 770680-1 / 板端座 776228-1 同规格体系，14/23/35 芯系列）；**具体品牌待选（未定）**。

## 1. 设备布局（已定）

| 设备 | 位置 | 外壳 |
|---|---|---|
| VCU | 车身中部 | **车规级铝壳** |
| 前桥板 | 前部 | 板级外壳（待定） |
| 后桥板 | 后部 | 同前桥板 |
| BMS | 中部下方（VCU 之下） | 电池箱内 |
| BCM | 车前部（上下方向的中间位置） | 板级外壳（待定） |
| 显示板 | 餐箱外（屏幕位） | 待定 |
| 上位机柜 | 待定 | — |

## 2. 电源分配（已定框架）

```
电池 48V（BMS）
  └─ 总空开（1 只）
       └─ 纯铜分线器（集中配电点）
            ├─ 大电流支路：伺服控制器 ×2（驱动）
            ├─ 大电流支路：转向关节电机 ×4（达妙，48V 直供）
            └─ 两路 → 隔离 DC 转换器
                 ├─ 24V 输出（负载清单待定）
                 └─ 12V 输出 → 12V 母线（各控制板、灯光、推杆、风扇、路由器、上位机、屏幕等）
                      └─ 5V：转换器输出或各板二次降压（具体分配待定）
```

- 隔离原则更新：低压系统经**隔离 DC 转换器**取得（用户确认），替代原"非隔离主 DCDC"方案
- 各 12V 支路保留独立保险（配电点处）
- 预充电路建议保留（空开合闸冲击保护），实施待定（见 04 文档）
- 功率线线型：纯铜铜芯、交联绝缘车规线；大电流支路按负载电流选截面（截面规格待核算）

## 3. 信号线束拓扑（不定长度，只定走向）

### 3.1 主干 CAN（500K）
- 屏蔽双绞线；从 VCU 引出，分别至：前桥板、后桥板、BCM、BMS（嘉佰达 CAN 口）
- 终端 120Ω ×2：VCU 端 + 最远节点端（布线确定后定哪端）
- 屏蔽层单点接地（VCU 端）

### 3.2 桥内 CAN
- 转向（1Mbps）：桥板 → 达妙 ×2（屏蔽双绞线；终端：桥板端 + 达妙端各 120Ω，达妙终端按其手册启用）
- 驱动（波特率待定）：桥板 → MSSD ×1
- 达妙 48V 供电**不经桥板**：从纯铜分线器直接拉线到电机（已定）

### 3.3 以太网（VCU 铝壳出线）
- **RJ45（千兆）**：带锁扣工业防水母座（车规级）→ 网线 → 上位机柜（已定走向）
- **T1 ×2**：双绞线均**引出**至车规防水母座（预留接惯导/臂视觉，设备未接入前堵防水帽）

### 3.4 传感器线束（前/后各一套）
- 超声波 ×4 + 激光 ×2：各自小芯数防水连接器 → 前/后桥板；单条线长 ≤50cm 原则（05 文档）
- 温湿度 ×2：舱内（餐箱内）+ 控制板层 → BCM（I2C 线）
- 摄像头（第一版 AHD）：同轴线 → 摄像头位（预留通道，AHD/GMSL 兼容）；帧同步触发线 ×2 从 VCU 引至摄像头位（必做）

### 3.5 其他
- 遥控接收机：安装位 → VCU（SBUS 线，5V 由 VCU 供给）
- 4G 模块天线：BCM 板上 IPEX → 天线安装位（同轴线）
- 显示板：电源 + UART（至上位机）线束
- 急停按钮：硬线直切链路（至驱动使能回路）+ 信号线至 BCM（双路径，06 §5）

## 4. 连接器分配（草案）

**体系**：板端 = PCB 插座（公针）；线束 = 预制母插头 + 压接端子线；对插后防水。芯数规格按 14/23/35 芯系列选。**具体品牌待选（未定）**，下表只定芯数与引脚内容。

| 板/设备 | 主连接器 | 芯数建议 | 引脚内容（草案） |
|---|---|---|---|
| VCU（铝壳） | 主座 ×1 + 网口 + T1 | 35 芯 | 主干 CAN（H/L）、12V 电源入（多芯并联）、遥控接收机、预留 ≥8 芯；RJ45 防水母座 ×1；T1 防水母座 ×2 |
| 前/后桥板 | 主座 ×1 + 传感器座 | 23 芯 + 小芯数传感器座 ×6 | 主干 CAN、桥内转向 CAN、桥内驱动 CAN、12V 电源入、5V 传感器电源出、预留；每个传感器独立 4 芯防水座 |
| BCM | 主座 ×1 + 功率出线组 | 35 芯 | 主干 CAN、12V 电源入、I2C 温湿度、上电时序控制脚、模式灯、预留；大功率负载（灯光组/风扇/功放/警灯/推杆/电磁锁）另用 2 芯防水座分组出线 |
| 显示板 | 小芯数座 ×1 | 14 芯 | 12V 电源、UART、背光控制、预留 |
| BMS | 按嘉佰达自带连接器 | — | 主干 CAN 对接线束 |

- 引脚明细（每芯定义）在布线评审后出正式针脚表
- 大电流回路（48V 支路）不走多芯信号连接器：经分线器 + 环形端子/车规大电流端子

## 5. 线束规整原则

1. 全车线束**预制化**：两头压好母头，现场只插不焊
2. CAN 一律屏蔽双绞线；功率线与信号线**分槽走**，交叉处直角交叉
3. 每根线束挂标识（编号规则：设备代号-连接器号-芯号，正式规则评审时定）
4. 过孔/穿板处用橡胶护圈；活动部位（舱盖）用高柔性线
5. 接地点统一规划：信号地单点接 VCU 安装点附近主接地点（具体位置布线时定）

## 6. 待确认/待定

1. 连接器品牌：待选（未定）；同规格体系下有多个品牌可选，选型时对比防水等级/端子镀层/供货与价格
2. 隔离 DC 转换器型号（48V→24V、48V→12V，功率按负载核算）与 24V 负载清单
3. 5V 分配方式（转换器第三路输出还是板级降压）
4. 各板外壳方案（桥板/BCM 的壳体）
5. 上位机柜位置（影响 RJ45 与显示板走向）
6. 预充电路实施方式（空开 + 预充）
