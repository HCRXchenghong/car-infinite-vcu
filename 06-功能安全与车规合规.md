# 06 功能安全与车规合规

## 1. 车规级的四个维度

| 维度 | 要求 | 本项目对应 |
|---|---|---|
| 器件级 | AEC-Q100/Q200，温度 -40~85/125°C，关键料双源（目标，核对项已登记） | **四块 S32K3 控制板**（VCU=S32K358，前/后桥板、BCM=S32K344/312，Grade 1）按**目标车规**设计，验证项见 §8 审计清单；成品执行器（达妙/伺服/嘉佰达）走系统级论证 |
| 功能安全 | ISO 26262，危害分析（HARA）→ 安全目标 → 安全机制 | 见第 2、3 节 |
| 环境/可靠性 | EMC（ISO 7637/11452、CISPR 25）、振动、盐雾、温湿度、IP 防护 | 整车 ≥IP54，接口处更高；🔲 测试待排期（开放项，见 §9） |
| 软件规范 | MISRA C、静态分析、单测覆盖、分层架构、诊断（UDS/ISO 14229）、OTA | S32K3 生态支持；🔲 流程待建立（开放项，见 §9） |

## 2. 安全目标与 HARA

### 2.1 安全目标表

| 危害场景 | 安全目标 | 等级参考 | 备注 |
|---|---|---|---|
| 驱动失控（意外加速/不停车） | 可控停车 | ASIL-C/D | 见 §2.2 打分 |
| 转向失控 | 限速 + 可控制动 | ASIL-C | 同上 |
| 制动失效 | 冗余制动 = **机械抱闸（断电抱死，待选型，见 07）** | ASIL-D | ⚠️ **抱闸选型完成前本目标不可验证**（原"电机反接"表述删除——全库无设计支撑） |
| 电池过充/过放/短路起火 | BMS 多级保护 + 保险丝 | ASIL-C | |
| 路面落差跌落（送餐车特有） | 提前检测 + 急停 | ASIL-B/C | 检测链见 05 §4.1 联算 |

### 2.2 HARA 打分表（草案待评审）

打分依据：S（严重度）S0 无伤害 / S1 轻 / S2 重 / S3 危及生命；E（暴露度）E0 几乎不出现 / E1 极低 / E2 低 / E3 中 / E4 高；C（可控性）C0 人人可控 / C1 多数可控 / C2 部分可控 / C3 基本不可控。ASIL 由 S/E/C 组合推定。

| 安全目标 | S | E | C | ASIL | 打分理由 |
|---|---|---|---|---|---|
| 驱动失控 → 可控停车 | S3 | E4 | C2 | ASIL D | S3：车体撞行人/骑手有致命风险；E4：人行道混行、取餐口近人，暴露极频繁；C2：行人几乎无法规避，仅靠周边人员干预 |
| 转向失控 → 限速 + 可控制动 | S3 | E4 | C3 | ASIL D（工程按 ASIL C 基线设计，分解后论证） | S3：失控方向可撞人；E4：同混行场景；C3：转向轴直接决定轨迹，基本不可控 |
| 制动失效 → 冗余制动（机械抱闸） | S3 | E3 | C3 | ASIL D | S3：无法制动等于持续运动风险；E3：坡道/满载/路口工况；C3：无可用替代制动手段 |
| 电池热失控 → 多级保护 + 保险丝 | S3 | E1 | C2 | ASIL C | S3：起火危及生命；E1：热失控概率低且有多级前置保护；C2：起火前通常有异味/烟气警示，可远离 |
| 路面落差跌落 → 提前检测 + 急停 | S2 | E3 | C2 | ASIL B | S2：低速车跌落以财产损失与轻伤为主；E3：路沿/坑洞为送餐路线常态；C2：落差可提前检测并急停反射 |

### 2.3 FTTI（故障容错时间间隔，正式时序要求）

全库既有时序基线：桥端反射切断 **50ms**、指令/心跳超时 **200ms**、感知超时 **500ms**（见 10 号矩阵 §5）。据此定义各安全目标的 FTTI：

| 安全目标 | FTTI 要求 | 依据与分解 |
|---|---|---|
| 驱动失控 → 可控停车 | **≤200ms**（桥端反射路径 ≤50ms） | 桥端本地反射：危险信号 → 50ms 内切本桥驱动；指令超时路径：200ms 斜坡停车；硬线急停路径为 μs~ms 级、不占软件预算 |
| 转向失控 → 限速 + 可控制动 | **≤200ms** | 达妙失联行为"保持当前位置"+ 桥端 100~200ms 心跳监测切驱动使能 |
| 制动失效 → 机械抱闸 | **≤100ms**（抱闸断电锁死时间，**待抱闸选型后按厂家参数回填**） | 断电抱死为物理动作，不经软件；选型完成前本项不可验证 |
| 电池异常 → 多级保护 | **≤1000ms** | BMS 硬件切断为其自身响应；主干侧以 0x5xx 超时 1000ms 降功率/报警（10 号矩阵 §5） |
| 路面落差 → 提前检测 + 急停 | 检测链 ≤200ms + 提前量（前探距离 ÷ 车速） | 检测端到端延迟见 05 §4.1；提前量不足的高速档执行"异常即减速" |

## 3. 三层防线（整车安全骨架）

本项目执行层为成品非车规控制器（达妙/伺服/嘉佰达），通过**系统级三层防线**论证安全性：

```
第 1 层：执行器自保护
  · 伺服控制器：过流跳闸 36~48A（电机额定 3~4 倍，定案见 03 §2.4）、堵转保护、通讯断线保护
  · 达妙电机：过温/过流自保护、失联行为配置（保持当前位置）
  · BMS：过充/过放/过流/短路/过温硬件切断

第 2 层：桥端否决（本地急停反射）
  · 物理上独立于 VCU 的安全路径
  · 传感器危险信号 → 桥端直接切断本桥驱动
  · 执行器/VCU 心跳丢失 → 本桥安全停车

第 3 层：机械兜底 + 硬线急停
  · 驱动轮独立机械抱闸（断电抱死型）：不依赖任何电子系统
  · 急停按钮（口径与 §5.2 定案一致）：硬线切断两台 MSSD 使能/供电
    + 断抱闸供电（断电抱死）；不切主接触器；信号镜像进
    BCM GPIO 并经 CAN 0x001（ESTOP_STATE）广播
```

## 4. 桥端本地急停反射逻辑（状态机）

| 触发输入 | 本地动作 | 动作时限 | 上报 |
|---|---|---|---|
| 激光测距发现落差 > 阈值（悬崖/深坑） | 本桥驱动切断 | **50ms** | 紧急事件帧（悬崖/落差） |
| 超声波近距障碍（按当前运动方向选对应侧探头；**倒车时后侧探头镜像触发**，见 05 §3.3） | 本桥驱动斜坡停车 | ≤100ms | 障碍事件帧 |
| 侧向超声波近距（斜移/原地转向工况） | 本桥驱动斜坡停车 | ≤100ms | 障碍事件帧（侧向） |
| 急停已激活（收 0x001 EstopActive=1） | 本桥断驱动使能 + **转向保持当前位并停止跟踪指令** | 即时（下一控制周期，≤20ms） | 状态帧置急停标志位 |
| 任一执行器心跳丢失 | 本桥全部断使能 | 100~200ms | 故障帧 |
| VCU 心跳丢失 > 200ms | 全桥斜坡停车（不保持旧指令） | 200ms | — |
| 桥端自身自检失败（ECC/看门狗） | 复位前断使能 | 即时 | 复位后故障码 |

## 5. 急停系统（已确定）

### 5.1 设计原则
- 急停必须不依赖任何软件即可生效：**硬线直切 + 状态记录双路径**

### 5.2 硬件路径（已确定）
- 急停按钮：常闭触点、防水型，安装在车体外侧随手可及处（取餐侧）
- **硬线路径**：急停按钮 → 硬线切断两台 MSSD 使能/供电 + 断抱闸供电（断电抱死）；**不切主接触器**——不经过任何 MCU，按下即断驱动
- **信号路径**：信号镜像进 **BCM GPIO**（记录急停状态、上报 VCU/上位机、触发告警），并经 **CAN 0x001（ESTOP_STATE）广播**全体节点执行安全动作

### 5.3 抱闸配合（已确定）
- 驱动轮抱闸选**断电抱死型**：急停切断供电的瞬间抱闸自动锁死车轮，形成无需电子系统的冗余制动（抱闸选型与供电路径见 §9 开放项）

### 5.4 急停后的转向处置（有意设计）
- **急停不断转向电源**：硬线只切两台 MSSD 使能/供电与抱闸供电，达妙转向供电保持——目的是急停复位后无需人工搬动即可**摆正车体**（四轮转向回到直行位）
- **转向停止跟踪指令由桥端执行**：桥端收到 0x001 EstopActive=1 后，转向保持当前位置、停止跟踪新指令（见 §4 状态机）；达妙失联行为本身也配置为"保持当前位置"，双保险
- **风险与对策**：急停期间转向仍有电，存在被误指令驱动的风险——对策为急停期间转向指令使能联锁（仅维护模式允许点动摆正），联锁逻辑由桥端实现

### 5.5 远程急停（已确定定义）
- **远程急停 = 上位机经 VCU 下发的软件急停路径**：触发后经 BCM 广播 0x001，**EstopSource=2**（远程指令，见 10 号矩阵 §4）
- **任何模式下有效**（自动驾驶/远程接管/本地遥控均响应），与硬线急停**并存**
- **硬线路径优先级最高且不受软件状态影响**：硬线直切使能 + 断抱闸供电，不经任何 MCU；软件路径需经桥端状态机执行，软件失效时由硬线 + 抱闸兜底

### 5.6 复位规则（已确定）
- 急停后必须：手动旋出复位按钮 + 上位机/VCU 确认 + 系统自检通过，才恢复行驶能力；恢复时斜坡起步，不允许直接满速

## 6. 信息安全（ISO 21434）

- S32K358/344 均带 **HSE-B 安全引擎**：安全启动、密钥存储、AES/RSA/ECC
- 规划：**SecOC 报文认证**（防 CAN 总线注入伪造加速指令——无人车真实风险）。备注：若启用 SecOC，50Hz 周期帧的认证开销需**重算总线负载**（现估算 ≈19%（10 v0.3.1 拆帧并补侧向帧后，见 10 §8），目标 <30%）
- OTA 升级：A/B 双区 + 签名校验 + 回滚（S32K3 FOTA 原生支持）

## 7. 失效场景与对策汇总

| 失效 | 检测 | 对策 |
|---|---|---|
| VCU 跑飞 | 锁步核硬件比对 + 外部窗口看门狗 | 微秒级检出复位；桥端心跳超时自行停车 |
| 主干 CAN 断 | 桥端心跳超时 | 桥端斜坡停车 |
| 单转向电机失联 | 桥端监控状态帧 | 切本桥驱动使能，四轮转向降级由机械限位保底 |
| 单驱动电机堵转 | 伺服堵转保护 | 断该电机输出，上报 |
| BMS 切断 | 全车掉电 | 机械抱闸（断电抱死）兜底 + 电容维持桥端短暂安全逻辑（🔲 设计评估，见 §9） |
| 传感器全失效 | 多路超时 | 停车 |

## 8. 审计证据清单（开发过程中持续积累）

- [ ] 危害分析与风险评估（HARA）报告（§2.2/§2.3 转正后归档）
- [ ] 安全目标 → 安全需求 → 安全机制的追溯矩阵（含 FTTI 时序验证记录）
- [ ] 成品执行器（达妙/伺服/嘉佰达）规格书与配置记录存档
- [ ] 三层防线验证用例与测试报告（故障注入）
- [ ] 电芯筛选记录、电池装配记录
- [ ] EMC/环境测试报告（量产阶段）
- [ ] 软件：MISRA 检查报告、单测覆盖率报告、版本发布记录

## 9. 待登记进 07 的开放项（供后续统一登记）

以下条目由本文档产生，待统一登记进 07 号文档（本轮不直接改 07）：

| # | 开放项 | 出处 |
|---|---|---|
| 1 | EMC/环境测试排期（ISO 7637/11452、CISPR 25、振动、盐雾、温湿度） | §1 |
| 2 | 软件规范建立（MISRA/静态分析/单测流程） | §1 |
| 3 | BMS 切断后桥端后备供电评估（电容维持安全逻辑） | §7 |
| 4 | 非车规成品件（达妙/伺服/嘉佰达）系统级安全论证骨架 | §1、§3 |
| 5 | 机械抱闸选型（断电抱死型；选型完成前 §2.1 制动失效目标不可验证，§2.3 抱闸锁死时限待回填） | §2.1、§5.3 |
| 6 | 抱闸供电路径确认（急停硬线断抱闸供电的具体走线与器件） | §5.2 |
