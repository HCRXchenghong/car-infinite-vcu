# 06 功能安全与车规合规

## 1. 车规级的四个维度

| 维度 | 要求 | 本项目对应 |
|---|---|---|
| 器件级 | AEC-Q100/Q200，温度 -40~85/125°C，关键料双源 | 自研三板全车规（S32K358/344，Grade 1）；成品执行器走系统级论证 |
| 功能安全 | ISO 26262，危害分析（HARA）→ 安全目标 → 安全机制 | 见第 2、3 节 |
| 环境/可靠性 | EMC（ISO 7637/11452、CISPR 25）、振动、盐雾、温湿度、IP 防护 | 整车 ≥IP54，接口处更高；🔲 测试待排期 |
| 软件规范 | MISRA C、静态分析、单测覆盖、分层架构、诊断（UDS/ISO 14229）、OTA | S32K3 生态支持；🔲 流程待建立 |

## 2. 安全目标（HARA 草案）

| 危害场景 | 安全目标 | 等级参考 |
|---|---|---|
| 驱动失控（意外加速/不停车） | 可控停车 | ASIL-C/D |
| 转向失控 | 限速 + 可控制动 | ASIL-C |
| 制动失效 | 冗余制动（电机反接 + 机械抱闸） | ASIL-D |
| 电池过充/过放/短路起火 | BMS 多级保护 + 保险丝 | ASIL-C |
| 路面落差跌落（送餐车特有） | 提前检测 + 急停 | ASIL-B/C |

## 3. 三层防线（整车安全骨架）

本项目执行层为成品非车规控制器（达妙/伺服/嘉佰达），通过**系统级三层防线**论证安全性：

```
第 1 层：执行器自保护
  · 伺服控制器：过流 40~45A、堵转保护、通讯断线保护
  · 达妙电机：过温/过流自保护、失联行为配置
  · BMS：过充/过放/过流/短路/过温硬件切断

第 2 层：桥端否决（本地急停反射）
  · 物理上独立于 VCU 的安全路径
  · 传感器危险信号 → 桥端直接切断本桥驱动
  · 执行器/VCU 心跳丢失 → 本桥安全停车

第 3 层：机械兜底
  · 驱动轮独立机械抱闸：不依赖任何电子系统
  · 急停按钮：断驱动使能 + 断主接触器（双动作）
```

## 4. 桥端本地急停反射逻辑（状态机）

| 触发输入 | 本地动作 | 上报 |
|---|---|---|
| 激光测距发现落差 > 阈值（悬崖/深坑） | 本桥驱动 **50ms 内切断** | 紧急事件帧 |
| 超声波 < 0.3m 且正在前进 | 本桥驱动斜坡停车 | 障碍事件帧 |
| 任一执行器心跳丢失 | 本桥全部断使能 | 故障帧 |
| VCU 心跳丢失 > 200ms | 全桥斜坡停车（不保持旧指令） | — |
| 桥端自身自检失败（ECC/看门狗） | 复位前断使能 | 复位后故障码 |

## 5. 急停系统（已确定）

### 5.1 设计原则
- 急停必须不依赖任何软件即可生效：**硬线直切 + 状态记录双路径**

### 5.2 硬件路径（已确定）
- 急停按钮：常闭触点、防水型，安装在车体外侧随手可及处（取餐侧）
- **硬线路径**：触点串入驱动使能回路，直接切断两台伺服控制器（MSSD）的使能/供电——不经过任何 MCU，按下即断驱动；同时断开抱闸供电回路（配合断电抱死型抱闸）
- **信号路径**：一路信号接入 BCM GPIO——记录急停状态、上报 VCU/上位机、触发告警与抱闸动作登记

### 5.3 抱闸配合（已确定）
- 驱动轮抱闸选**断电抱死型**：急停切断供电的瞬间抱闸自动锁死车轮，形成无需电子系统的冗余制动

### 5.4 复位规则（已确定）
- 急停后必须：手动旋出复位按钮 + 上位机/VCU 确认 + 系统自检通过，才恢复行驶能力；恢复时斜坡起步，不允许直接满速

## 6. 信息安全（ISO 21434）

- S32K358/344 均带 **HSE-B 安全引擎**：安全启动、密钥存储、AES/RSA/ECC
- 规划：**SecOC 报文认证**（防 CAN 总线注入伪造加速指令——无人车真实风险）
- OTA 升级：A/B 双区 + 签名校验 + 回滚（S32K3 FOTA 原生支持）

## 7. 失效场景与对策汇总

| 失效 | 检测 | 对策 |
|---|---|---|
| VCU 跑飞 | 锁步核硬件比对 + 外部窗口看门狗 | 微秒级检出复位；桥端心跳超时自行停车 |
| 主干 CAN 断 | 桥端心跳超时 | 桥端斜坡停车 |
| 单转向电机失联 | 桥端监控状态帧 | 切本桥驱动使能，四轮转向降级由机械限位保底 |
| 单驱动电机堵转 | 伺服堵转保护 | 断该电机输出，上报 |
| BMS 切断 | 全车掉电 | 机械抱闸兜底 + 电容维持桥端短暂安全逻辑（🔲 设计评估） |
| 传感器全失效 | 多路超时 | 停车 |

## 8. 审计证据清单（开发过程中持续积累）

- [ ] 危害分析与风险评估（HARA）报告
- [ ] 安全目标 → 安全需求 → 安全机制的追溯矩阵
- [ ] 成品执行器（达妙/伺服/嘉佰达）规格书与配置记录存档
- [ ] 三层防线验证用例与测试报告（故障注入）
- [ ] 电芯筛选记录、电池装配记录
- [ ] EMC/环境测试报告（量产阶段）
- [ ] 软件：MISRA 检查报告、单测覆盖率报告、版本发布记录
