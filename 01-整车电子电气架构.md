# 01 整车电子电气架构

## 1. 架构总览

整车采用**"中央决策 + 桥端安全网关"**两级架构：

```
                 自动驾驶上位机（决策规划，非底盘范围）
                          │
                    ┌─────┴──────┐
                    │  VCU: S32K358 │  唯一决策者
                    └─┬─────┬─────┬─┐
           主干 CAN 500K │     │     │ │
            ┌────────────┘     │     │ └───────────┐
            ▼                  ▼     ▼             ▼
     前桥 MCU: S32K344   后桥 MCU: S32K344   BMS（嘉佰达）  BCM: S32K312
     · 安全网关+急停反射  · 安全网关+急停反射             · 上盖开合/灯光/远程开机/警灯功放/5G路由器电源
     · 前传感器汇聚(6路)  · 后传感器汇聚(6路)
            │                  │
     桥内 CAN(1M/厂家)    桥内 CAN(1M/厂家)
     ├ 2× 达妙 J10010     ├ 2× 达妙 J10010
     └ 1× MSSD 伺服       └ 1× MSSD 伺服
        （前2轮毂电机）       （后2轮毂电机）
```

## 2. 节点职责定义

### 2.1 VCU（S32K358）—— 唯一决策者

- 接收上位机运动指令（目标速度、转向模式）
- 四轮转向运动学解算：目标车速/转向半径 → 四轮转角 + 四轮转速
- 避障决策：汇总前后桥传感器数据，输出减速/停车指令
- 整车功率管理：总电流逼近上限时优先降驱动、保转向
- 高压上下电时序管理（主接触器 + 预充）
- 诊断中心：UDS 刷写、故障码收集、节点心跳监控
- 桥端/执行器失联时的全局安全策略

### 2.2 前后桥 MCU（S32K344 ×2）—— 安全网关 + 本地反射

- 接收 VCU 目标指令，转发给本桥执行器（达妙 ×2 + 伺服控制器 ×1）
- 汇聚本桥 6 路 UART 传感器（4 超声波 + 2 激光），预处理后打包上报
- **本地急停反射**：传感器危险信号直接切断本桥驱动，不等 VCU（详见 06 文档）
- 监控本桥执行器心跳 + VCU 心跳，失联即安全停车
- **铁律：桥端没有"决策权"，只有"执行权 + 往安全方向的否决权"**

### 2.3 BCM（S32K312，料号 S32K312NHT0MPBST）—— 车身控制器

- 上盖开合（伸缩电动推杆 + 电磁锁，带锁状态反馈）、灯光开关、警灯/功放、5G 路由器电源管理、整车远程开机执行（详见 `08-车身控制器（BCM）方案.md`）
- 4G 常电唤醒模块直接接入 BCM，待机监听唤醒指令并执行上电时序（具体模块与接口待定）
- 转向灯/刹车灯由 VCU 经 CAN 请求、BCM 驱动外置功率级点亮（转向灯为通电自闪型，BCM 只需通断；功率级器件待选型）
- 对讲/喊话音频流归上位机，BCM 只负责麦克风/功放供电与使能
- 接入主干 CAN，受上位机/VCU 指令控制
- 不参与行驶安全链路（QM 域）
- 其他待确认设计（显示屏归属、防夹方案等）见 08 文档第 7 节

### 2.4 执行器（成品控制器）

- 达妙 J10010 ×4：转向，桥内 CAN 1Mbps
- MSSD-100EMA ×2：驱动，一拖二带 4 轮毂电机，桥内 CAN（波特率待厂家确认）
- 自身带一级保护：过流、堵转、过温（配置见 03 文档）

### 2.5 BMS（嘉佰达）

- 电池一级保护（过充/过放/过流/短路/过温硬件切断）
- 经主干 CAN 上报 SOC、总压、总流、温度、故障码（协议文档待厂家提供）

## 3. CAN 网段划分

| 网段 | 速率 | 节点 | 用途 |
|---|---|---|---|
| **主干总线** | 500K | VCU、前桥 MCU、后桥 MCU、BMS、BCM | 指令/状态/心跳/急停/诊断/车身控制 |
| **前桥内转向** | 1Mbps | 前桥 MCU、2× 达妙 | 达妙固定 1M，高频控制独占 |
| **后桥内转向** | 1Mbps | 后桥 MCU、2× 达妙 | 同上 |
| **前桥内驱动** | 待确认 | 前桥 MCU、1× MSSD | 控制指令 + 编码器反馈 |
| **后桥内驱动** | 待确认 | 后桥 MCU、1× MSSD | 同上 |

> 注：驱动网段是否单独分线，或与转向合并成每桥一根总线，视 MSSD 波特率与总线负载实测后定稿（合并的前提是速率一致且负载 <60%）。

### 分网理由

1. 达妙固定 1Mbps，与 500K 总线不能混挂，必须独立
2. 转向高频流量（250~500Hz）封闭在桥内，不上主干
3. 主干短路只影响通信，不直接丢执行器（桥端本地仍可安全停车）
4. 前后桥物理隔离：前桥故障不牵连后桥

## 4. 通信负载预算

**转向桥内总线（1Mbps）**：
- 2 电机 × 2 帧（命令+反馈）× 250Hz = 1000 帧/秒 ≈ **~20% 负载** ✅
- 若跑 500Hz 则约 40%，仍健康；建议控制周期从 250Hz 起步

**主干总线（500K）**：
- 桥端上报（传感器包 + 状态 + 心跳）≈ 每桥 5~10 帧 @20~50Hz
- VCU 下发（指令）≈ 每桥 2~3 帧 @20~50Hz
- BMS 状态 ≈ 2~3 帧 @1~5Hz
- 合计负载极低（<15%），富余量大 ✅

## 5. 心跳与超时策略（骨架）

| 监控方 | 被监控对象 | 超时阈值（建议） | 超时动作 |
|---|---|---|---|
| VCU | 前/后桥 MCU | 200ms | 全车限速/停车，上报故障 |
| 桥端 | VCU | 200ms | 本桥斜坡停车 |
| 桥端 | 达妙电机状态帧 | 100~200ms | 切本桥驱动使能 |
| 桥端 | MSSD 状态 | 100~200ms | 切本桥驱动使能 |
| VCU | BMS | 1000ms | 降功率运行，报警 |

> 具体数值在 CAN 报文矩阵设计中定稿（见 07 待办）。

## 6. 控制链路数据流（典型场景）

**正常行驶**：
```
上位机 →(目标速度/航向) VCU →(四轮转角+转速) 桥端 →(转发) 达妙/伺服 → 电机
                            ↑(传感器包+状态)  桥端 ←(UART) 传感器
```

**悬崖急停（本地反射）**：
```
激光测距异常 → 桥端本地 50ms 内切驱动 → 同时上报紧急事件帧 → VCU 全局处置
```

**通信全断**：
```
桥端监测 VCU 心跳丢失 >200ms → 本桥斜坡停车（不保持旧指令运行）
```

## 7. 工作模式与控制仲裁（已确定）

### 7.1 三种模式与优先级（已确定）
| 模式 | 控制路径 | 优先级 |
|---|---|---|
| 本地遥控 | 2.4G 遥控器接收机（SBUS 输出）→ **直连 VCU** UART | **最高** |
| 远程接管 | 5G → 上位机 → CAN → VCU | 中 |
| 自动驾驶 | 上位机自主栈 → CAN → VCU | 低 |

- 遥控接收机直连 VCU 的理由：本地遥控是现场操作员的保命接管手段，即使上位机死机也必须有效，因此不能走上位机链路

### 7.2 仲裁规则（VCU 实现，已确定）
1. 任何时刻只有一个控制源有效；遥控档位强制优先（覆盖一切）
2. 模式切换通过遥控器三档开关；远程接管请求需上位机/VCU 接管确认
3. 模式切换时车辆必须先斜坡减速（不允许高速中直接切换控制源）
4. 每个控制源带心跳：自动驾驶/远程接管心跳丢失 → 停车；遥控信号丢失 → 停车
5. 当前模式由 BCM 驱动模式指示灯（三色）显示，远程端同步可见

### 7.3 硬件新增
- 2.4G 遥控套件（ELRS/Flysky 类，SBUS 输出），接收机接 VCU UART
- 急停按钮不受模式仲裁约束：硬线直切，任何模式下都有效（见 06 §5）

## 8. 上位机感知/定位设备（不入底盘控制链，接口预留）

- **激光雷达：Livox Mid-360**（已确认）：经 **100M 以太网**接上位机（ROS 驱动在上位机运行）；供电 12V（按设备规格）
- **定位：RTK 融合 IMU 设备**（已确认华测或导远二选一）：输出接口按所选型号定（常见串口/以太网），接上位机；选型时确认接口与协议，底盘侧预留 12V 供电
- 两设备数据均直进上位机，不经过底盘控制链；整车组网经车载交换机（车上已有设备）
- **时间同步（参考；主时钟由上位机组负责）**：工控机跑 linuxptp（ptp4l）作 gPTP 主时钟，Mid-360 为 PTP 从时钟自动同步（Livox 官方支持 PTP/gPTP/PPS，点云包头 timestamp_type=1 表示生效）；惯导设备支持 gPTP/CANtsync/PPS（按选型）
- **RTK 融合惯导接底盘 CAN（底盘组职责，方向已确认）**：
  - **轮速广播双层设计（已确定，针对四轮四转）**：
    · 原始层：四轮轮速（轮毂编码器实测）+ 四轮实际转角（达妙编码器实测），50Hz，供上位机轮式里程计/融合校验（四轮四转必须带转角才能重建车体系速度）
    · 合成层：VCU 运动学解算的车体纵向车速标量，50Hz，供惯导（匹配惯导"车速通道"输入格式）
    · 原则：只用实测值（禁用指令值）；周期稳定 + 新鲜度判断；转角反馈异常时置无效标志位
  - 惯导输出定位数据至 CAN，上位机取用；惯导接底盘主干 CAN（500K，与 TJA1443 兼容）或预留 CAN 段
  - 候选已确认：华测 CGI-230（串口/100Base-T1 车规以太网/CAN-FD，支持 gPTP/CANtsync/PPS，AUTOSAR 架构）、导远 INS570 系列（CAN + PPS）；协议以选型手册为准（待办见 07）
  - 若惯导走以太网：华测 CGI-230 网口为 100Base-T1 单对线（印证 VCU 板预留 TJA1103 的必要性）；研发期走 CAN/串口最简
- **机械臂（后期自研，达妙电机 + 视觉摄像头，带视觉已确认）**：
  - 控制链：预留独立 1M CAN 网段（与转向同构，达妙电机控制）
  - 视觉数据链路：未定（不一定走车规 T1）；VCU 侧已预留 **2×车规 T1 口 + 1×千兆口**（TJA1104 + SJA1105Q 焊盘），供惯导/臂视觉等待用
- **摄像头演进（第一版 AHD → 后续 GMSL，已确认）**：
  - AHD 与 GMSL 均走同轴线：**整车预留同轴走线通道与连接器位，迁移不换线**（线束与功率线分槽）
  - 底盘侧预留：① 摄像头组 12V 供电支路（BCM 继电器控制，继电器预留 +1 共 8 只，待机断电）；② 帧同步触发线 ×2（**必做**，VCU GPIO ×2 引线到摄像头位，GMSL 方案需硬件同步时使用）；③ 解串器板装位与供电（近上位机安装，底盘提供装位）
  - 摄像头数据不经底盘 CAN：同轴 → 解串器板 → 上位机（MIPI CSI/PCIe）
