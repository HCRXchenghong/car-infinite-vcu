# BCM 板硬件设计（S32K312）

> 状态：设计准备。依据 `08-车身控制器（BCM）方案.md`、`09-全车供电与线路分配.md` 已确定内容。
> 本板是五块自研板中功率输出最多的一块，布局上**功率区与逻辑区严格分开**。

## 1. 板卡定位

- 车身电器执行中心：上盖机构、灯光、5G 路由器/上位机/屏幕电源、风扇、功放、警灯
- 常电待机节点：4G 唤醒守夜 + 整车上电时序执行（08 §5.1）
- 舱内环境监测（温湿度）与模式指示
- 不参与行驶安全链路（QM 域）

## 2. 主控（已确定）

- **NXP S32K312NHT0MPBST**（单核 M7 @120MHz，2MB Flash，6×CAN-FD，8×UART，77 GPIO，HDQFP-172，12NC 935436148557）
- 开发板：S32K312EVB-Q172

## 3. 电源树

```
12V 输入（防反接 + TVS + 支路保险）
├─ 12V→5V buck ≥2A
│   ├─ 5V→3.3V（MCU VDD）
│   ├─ 5V→~4V buck + 大电容（4G 模块专用，搜网峰值 ~2A，禁 LDO）
│   ├─ I2C 上拉 / 传感器供电（SHT31 ×2）
│   └─ CAN 收发器 5V
├─ 12V 功率输出区（见第 5 节）：经高边开关/继电器输出，继电器线圈 12V 直驱 + 三极管开关控制，线圈并联续流二极管；继电器具体型号待定，触点容量按负载选；线圈控制信号来自 MCU GPIO，经三极管/小 MOS 驱动
└─ 常电设计：4G 模块 + MCU 待机域常供电；其余功率输出默认断电，按上电时序逐级开启；电源管理方案（已定）：车规 SBC（NXP FS26，含窗口看门狗/复位/电压监控），与全车统一
```

## 4. 接口分配（外设级）

| 功能 | 外设 | 数量 | 说明 |
|---|---|---|---|
| 主干 CAN（500K） | FlexCAN | 1 | TJA1443（已定，与其他板同料） |
| 预留 CAN | FlexCAN | 1 | 扩展（机械臂等） |
| 4G 模块（EG800Z-GL，**背部引脚 LGA 裸模组，直接焊接，已确认**） | LPUART | 1 | AT 指令 + 唤醒引脚（GPIO） |
| 显示板接口 | — | 0 | 已定：显示板内容由上位机（临滴 RK3588）直连，不经本板转发 |
| 温湿度传感器 ×2（SHT31） | LPI2C | 1 路（两器件共总线） | 舱内 + 控制板层 |
| 推杆电流采样 | ADC | 1 | MC33926 低边采样电阻 → 差分放大 → ADC（防夹） |
| 电磁锁状态反馈 | GPIO | 1 | 无磁信号反馈（08 §5.3） |
| 急停按钮状态 | GPIO | 1 | 硬线链路的信号镜像（06 §5） |
| 模式指示灯（三色） | GPIO | 3 | 自动驾驶/远程接管/本地遥控（01 §7） |
| 高边开关诊断 | LPSPI | 1 | BTS7004 SPI 诊断 |
| GPIO 预留 | GPIO | ≥6 | |
| 时钟 | FXOSC | 8~40MHz（待定） | |

## 5. 功率输出通道（已确定功能，器件部分待定）

| 负载 | 驱动器件 | 状态 |
|---|---|---|
| 灯光（清单已定）：前 LED 矩阵灯×2、前转向灯×2、后转向灯×2、舱内灯×2、后尾灯×2、自动驾驶蓝灯×2、刹车灯×2（后补）、冗余×2，共 16 通道（转向灯为通电自闪型；刹车灯由 VCU 经 CAN 请求） | **Infineon BTS7004-1EPP ×4 片**（每片 4 通道，共 16 通道，占满） | 器件与数量已定；LED 矩阵灯按整灯通断驱动（用户确认）；通道已无冗余，后续加灯需增加高边开关片数 |
| 电磁锁 | 复用高边开关通道 + 续流二极管 | 器件方案已定 |
| 上盖推杆（12V） | **NXP MC33926**（H 桥 5A）+ 低边采样电阻 | 器件已定 |
| 5G 路由器电源 | 继电器（车规，型号待定） | |
| 上位机电源 | 继电器（型号待定） | |
| 显示板电源 | 继电器（型号待定） | 待机时断电；**延迟上电使能线不采纳**（STM32 上电即播动效，无泄露风险，用户确认） |
| 静音风扇 | 继电器（型号待定） | 温湿度超阈触发 |
| 功放主电 | 继电器（型号待定） | |
| 警灯 ×2（通电自闪型） | 继电器 ×2（型号待定） | 只通电供电 |
| 摄像头组电源（第一版 AHD/后续 GMSL） | 继电器（型号待定） | 继电器预留 +1；待机断电（见 01 §8） |

> 继电器数量暂按 8 只预留（路由器/上位机/屏幕/风扇/功放/警灯×2/摄像头组），最终以使能方案定稿为准。

## 6. 外围电路清单

- 每只继电器：三极管/MOS 驱动 + 续流二极管 + 状态反馈（可选）
- CAN 保护（TVS + 共模电感）；电源管理（已定）：车规 SBC（NXP FS26），与全车统一
- 4G 模块：SIM 卡座（Nano）、天线接口（IPEX，天线位置整车定）、去容与大电容（≥470µF 级）
- 复位电路、看门狗策略与全车统一

## 7. 待确认问题

1. 继电器具体型号（车规、12V 线圈、触点容量按负载定）
2. ~~屏幕/背光延迟上电使能线~~ —— 已否决（不采纳，无需使能线，见 08 §5.4）
